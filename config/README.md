# System Configuration (`/config`)

This directory contains configuration files mapping ecosystem target repositories, clone URLs, and target paths.

---

## Contents

- `repos.json`: Target repository list and metadata.
