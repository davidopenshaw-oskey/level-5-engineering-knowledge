# Copy this file to .env and fill in real values. .env is gitignored -- never commit it.
# Gemini needs no key here -- it authenticates via Vertex AI + ADC (gcloud auth application-default login).

ANTHROPIC_API_KEY=
