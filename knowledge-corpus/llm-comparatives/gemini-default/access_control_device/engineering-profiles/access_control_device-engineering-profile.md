### 0. Generation Metadata

- runId: 20260803_143350-1aa319b1
- generatedAt: 2026-08-11T16:48:37.117Z
- repoName: firebase-oskey-dev
- targetModule: access_control_device
- llmConfigKey: gemini-default
- llmProvider: gemini
- llmModel: gemini-3.5-flash

### 1. Executive Summary

The `access_control_device` module is responsible for the lifecycle management, configuration, and cryptographic provisioning of physical Access Control Devices (ACDs), specifically Intercoms and Digicoms [Confirmed]. It acts as the primary bridge between cloud-native business logic and edge hardware, orchestrating configuration updates and public key synchronization via GCP Pub/Sub and external IoT APIs [Confirmed]. Additionally, the module enriches raw hardware-originated activity events with business context (buildings, doors, and users) to enable downstream logging, auditing, and access tracking across the platform [Confirmed].

### 2. Architectural Position

The `access_control_device` module sits at the boundary between the cloud platform and physical edge hardware. It owns the logical representation, configuration, and cryptographic state of physical devices installed at building entry points [Confirmed]. It provides core capabilities for device registration, configuration management, public key maintenance, and activity enrichment to administrative portals (PGO), installer applications, and downstream domain modules [Confirmed].

### 3. Primary Responsibilities

#### _module_root

### Device Lifecycle & Door Assignment
- **Device Registration & Management**: Handles saving, updating, and deleting core device documents under `/accessControlDevices` [Confirmed; `controller_method|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device.controller.ts|OSKAccessControlDeviceController|save|#1`].
- **Door Assignment**: Assigns and unassigns physical building doors to specific ACDs, updating the device's `buildingDoorAssignment` mapping [Confirmed; `controller_method|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device.controller.ts|OSKAccessControlDeviceController|assignBuildingDoor|#1`].
- **User Access Synchronization**: Triggers updates to user accesses when a device is assigned or unassigned from a door, ensuring that user permissions align with the active hardware footprint [Confirmed; `call_expression|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device.controller.ts|OSKAccessUpdateService.addAccessControlDeviceToUserAccessesDoor|assignBuildingDoor|doorId,buildingId,{                 accessControlDeviceId: accessControlDevice.accessControlDeviceId,                 accessControlDeviceIdShort: accessControlDevice.accessControlDeviceIdShort,             }|#1`].

### Configuration & Telemetry Management
- **Configuration Provisioning**: Manages the creation, deletion, and retrieval of device configurations under `/accessControlDevices/{id}/configs` [Confirmed; `source_class|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device_config.controller.ts|OSKAccessControlDeviceConfigController`].
- **State & Telemetry Tracking**: Persists real-time telemetry data from devices, including battery levels, humidity, temperature, power status, and local storage usage under `/accessControlDevices/{id}/states` [Confirmed; `model_property|access_control_device|functions/src/modules/access_control_device/models/documents/access_control_device_state_document.model.ts|OSKAccessControlDeviceState|batteryLevel|#1`].
- **System Logs**: Collects and stores system-level logs generated by the edge hardware under `/accessControlDevices/{id}/systemLogs` [Confirmed; `source_class|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device_system_logs.controller.ts|OSKAccessControlDeviceSystemLogsController`].

### Cryptographic Key Management
- **Public Key Administration**: Manages signing and encryption public keys used by devices to validate SecureBLE tokens and offline PINs [Confirmed; `source_class|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device_public_keys.controller.ts|OSKAccessControlDevicePublicKeysController`].
- **Callable Key Operations**: Exposes secure callable endpoints to add or delete public keys for a device, verifying caller permissions and App Check status [Confirmed; `api_contract|access_control_device|functions/src/modules/access_control_device/index.ts|onAddAccessControlDevicePublicKeyCalled|#1`].

### Activity Enrichment
- **Contextual Enrichment**: Resolves raw hardware-originated events (containing bare IDs) into fully contextualized activity records by looking up the associated building, door, and user details [Confirmed; `service_method|access_control_device|functions/src/modules/access_control_device/services/access_control_device_activity_enrichment.service.ts|OSKActivityEnrichmentService|enrichAndValidateActivity|#1`].
- **User Type Resolution**: Determines the actor type (e.g., standard user, supplier, resident) by matching the event's `accessId` or `userId` against building accesses and user profiles [Confirmed; `call_expression|access_control_device|functions/src/modules/access_control_device/services/access_control_device_activity_enrichment.service.ts|OSKBuildingAccessesController.default.get|enrichAndValidateActivity|buildingId,activityDocument.userId|#1`].

### 4. Public Interfaces

#### _module_root

This capability exposes several document controllers and services as public entry points:

### Document Controllers (Firestore)
- **`OSKAccessControlDeviceController`**: Manages `/accessControlDevices` documents, handling registration, door assignment, and unassignment [Confirmed; `functions/src/modules/access_control_device/controllers/access_control_device.controller.ts`].
- **`OSKAccessControlDeviceConfigController`**: Manages `/accessControlDevices/{id}/configs` documents and publishes configuration updates [Confirmed; `functions/src/modules/access_control_device/controllers/access_control_device_config.controller.ts`].
- **`OSKAccessControlDevicePublicKeysController`**: Manages `/accessControlDevices/{id}/publicKeys` documents and handles key rotation/publishing [Confirmed; `functions/src/modules/access_control_device/controllers/access_control_device_public_keys.controller.ts`].
- **`OSKAccessControlDeviceStateController`**: Manages `/accessControlDevices/{id}/states` documents [Confirmed; `functions/src/modules/access_control_device/controllers/access_control_device_state.controller.ts`].
- **`OSKAccessControlDeviceSystemLogsController`**: Manages `/accessControlDevices/{id}/systemLogs` documents [Confirmed; `functions/src/modules/access_control_device/controllers/access_control_device_system_logs.controller.ts`].
- **`OSKAccessControlDeviceAccessCommandsController`**: Manages `/accessControlDevices/{id}/accessCommands` documents [Confirmed; `functions/src/modules/access_control_device/controllers/access_control_device_access_commands.controller.ts`].

### IoT API Controllers & Services
- **`OSKAccessControlDeviceController` (IoT API)**: Handles edge device registration and unregistration requests [Confirmed; `functions/src/modules/access_control_device/api/node-iot-api/controllers/access_control_device.controllers.ts`].
- **`OSKNodeIoTAPIService`**: Communicates with the external Node IoT API to register/unregister devices and sign JWTs using Google Cloud IAM service accounts [Confirmed; `functions/src/modules/access_control_device/api/node-iot-api/services/node_iot_api.service.ts`].

### Business Services
- **`OSKActivityEnrichmentService`**: Public service used by event ingestion pipelines to enrich raw hardware activity logs with business context [Confirmed; `functions/src/modules/access_control_device/services/access_control_device_activity_enrichment.service.ts`].

### 5. Internal Structure

Based on the deterministic Intra-Module Coupling Graph, the `access_control_device` module contains no internal submodules (`submodules: {}`) [Confirmed]. All capabilities, services, and controllers are managed directly within the module root (`_module_root`) [Confirmed].

### 6. Firestore & Data Ownership

**Ownership conclusion:**

The `access_control_device` module is the primary system of record and authoritative owner of the `/accessControlDevices` collection and its subcollections (`/configs`, `/publicKeys`, `/logs`, `/states`) [Inferred]. This is supported by the fact that its controllers (`OSKAccessControlDeviceController` and `OSKAccessControlDeviceConfigController`) are the central points of access for these paths, called by multiple external modules including `admin`, `building`, `call`, and `organization` [Confirmed]. While other modules touch these paths or call these controllers to assign doors or update configurations, the core schema and lifecycle operations reside within this module [Inferred].

**Per-capability evidence:**

#### _module_root

### Firestore Paths Touched
This capability reads and writes data across the following Firestore paths:

- `/accessControlDevices/{deviceId}` [Confirmed; `firestore_path_touched|access_control_device|functions/src/modules/access_control_device/index.ts|/accessControlDevices/{deviceId}|#1`]
  - **Touch Type**: Path Reference
  - **Operation Detection Scope**: Undetermined (may be indirect)
- `/accessControlDevices/{deviceId}/configs/{configId}` [Confirmed; `firestore_path_touched|access_control_device|functions/src/modules/access_control_device/index.ts|/accessControlDevices/{deviceId}/configs/{configId}|#1`]
  - **Touch Type**: Path Reference
  - **Operation Detection Scope**: Undetermined (may be indirect)
- `/accessControlDevices/{deviceId}/publicKeys/{keyType}` [Confirmed; `firestore_path_touched|access_control_device|functions/src/modules/access_control_device/index.ts|/accessControlDevices/{deviceId}/publicKeys/{keyType}|#1`]
  - **Touch Type**: Path Reference
  - **Operation Detection Scope**: Undetermined (may be indirect)
- `accessControlDevices/{accessControlDeviceId}/configs` [Confirmed; `firestore_path_touched|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device_config.controller.ts|accessControlDevices/{accessControlDeviceId}/configs|#1`]
  - **Touch Type**: Path Reference
  - **Operation**: `get`
  - **Operation Detection Scope**: Direct chain detected (partial)

### 7-8. API Endpoints & Firestore Triggers

*(Combined for this assembly-first experiment -- the capability contract reports these together; a future revision could split them if the combined section proves unwieldy.)*

#### _module_root

### Callable Functions
The following callable functions are exported by this capability:

#### `onAddAccessControlDevicePublicKeyCalled`
- **Request Schema**: `OSKAccessControlDevicePublicKeyAddRequest` [Confirmed; `functions/src/modules/access_control_device/index.ts` (line 113)]
  - `deviceId`: `string`
  - `keyType`: `"signing" | "crypto"`
- **Response Schema**: No matching `model_property` facts found in this pack to represent the response schema [Inferred].

#### `onDeleteAccessControlDevicePublicKeyCalled`
- **Request Schema**: `OSKAccessControlDevicePublicKeyDeleteRequest` [Confirmed; `functions/src/modules/access_control_device/index.ts` (line 114)]
  - `deviceId`: `string`
  - `keyType`: `"signing" | "crypto"`
- **Response Schema**: No matching `model_property` facts found in this pack to represent the response schema [Inferred].

---

### Firestore Triggers
The capability registers the following Firestore triggers to react to document changes:

| Trigger Event | Target Path | Handler |
| :--- | :--- | :--- |
| `onCreate` | `/accessControlDevices/{deviceId}` | `OSKAccessControlDeviceService.onDocumentCreated` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentCreated|#1`] |
| `onDelete` | `/accessControlDevices/{deviceId}` | `OSKAccessControlDeviceService.onDocumentDeleted` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentDeleted|#1`] |
| `onCreate` | `/accessControlDevices/{deviceId}/configs/{configId}` | `OSKAccessControlDeviceConfigService.onDocumentCreated` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentCreated|#2`] |
| `onUpdate` | `/accessControlDevices/{deviceId}/configs/{configId}` | `OSKAccessControlDeviceConfigService.onDocumentUpdated` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentUpdated|#1`] |
| `onDelete` | `/accessControlDevices/{deviceId}/configs/{configId}` | `OSKAccessControlDeviceConfigService.onDocumentDeleted` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentDeleted|#2`] |
| `onCreate` | `/accessControlDevices/{deviceId}/publicKeys/{keyType}` | `OSKAccessControlDevicePublicKeysService.onDocumentCreated` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentCreated|#3`] |
| `onDelete` | `/accessControlDevices/{deviceId}/publicKeys/{keyType}` | `OSKAccessControlDevicePublicKeysService.onDocumentDeleted` [Confirmed; `firestore_trigger|access_control_device|functions/src/modules/access_control_device/index.ts|unknown|onDocumentDeleted|#3`] |

### 9. Permissions & Security

**Cross-cutting risk callouts:**

* **Enforcement Tally**: The module enforces a custom permission check `accessControlDevice.publicKey.maintain` during public key registration (`onAddAccessControlDevicePublicKeyCalled`) [Confirmed]. However, standard administrative operations (such as device registration, deletion, or configuration updates) handled by `OSKAccessControlDeviceController` and `OSKAccessControlDeviceConfigController` do not show explicit RBAC checks within the extracted code signatures of this module, indicating a reliance on upstream caller-level enforcement or implicit rules [Inferred].
* **RBAC Mismatch**: The permission string `accessControlDevice.publicKey.maintain` is actively enforced in code but is completely absent from the canonical `rbac-roles.json` schema [Confirmed]. This represents a critical security-relevant signal where the code enforces a permission that is undocumented and unmapped in the platform's standard RBAC roles [Confirmed].
* **App Check Guardrails**: Callable triggers enforce Firebase App Check in non-emulator environments to ensure only verified clients can invoke key management endpoints [Confirmed].

**Per-capability evidence:**

#### _module_root

### Permission Strings Referenced
- **`accessControlDevice.publicKey.maintain`**: Checked during the execution of `onAddAccessControlDevicePublicKeyCalled` to ensure the user has authority to maintain device public keys [Confirmed; `permission_error|access_control_device|functions/src/modules/access_control_device/services/access_control_device_public_keys.service.ts|permission-denied|#1`].

### RBAC Mismatch Analysis
- **Mismatch**: The permission string `accessControlDevice.publicKey.maintain` is **not** defined in the canonical `rbac-roles.json` document. The closest standard administrative permissions in the RBAC document are `v1.admin.accessControlDevice.edit` and `v1.admin.accessControlDevice.register`. This indicates a custom or undocumented permission string is being enforced in code for public key operations [Confirmed].

### Security Guardrails
- **App Check Enforcement**: Callable functions enforce Firebase App Check verification in non-emulator environments to prevent unauthorized clients from invoking key management endpoints [Confirmed; `call_expression|access_control_device|functions/src/modules/access_control_device/index.ts|functionBuilder.runWith|getCallableFunctionTriggers|{         enforceAppCheck: !process.env.OSK_FIREBASE_EMULATOR,     }|#1`].

### 10. Cross-Module Relationships

#### Outbound Dependencies
* **`core`**: High-density dependency (25 touchpoints) [Confirmed]. Calls `OSKLoggingService` for error/info/warning logging, `OSKDocumentAndMessageController` for generic CRUD operations and Pub/Sub publishing, and `OSKPublicKeysController` for key management [Confirmed].
* **`building`**: 5 touchpoints [Confirmed]. Calls `OSKBuildingAccessesController.get` to retrieve access rights, and `OSKBuildingController.getSafe` / `OSKBuildingDoorController.getSafe` to resolve physical context during activity enrichment [Confirmed].
* **`organization`**: 2 touchpoints [Confirmed]. Calls `OSKOrganizationController.get` to fetch organization context for public key operations [Confirmed].
* **`user`**: 2 touchpoints [Confirmed]. Calls `OSKUserController.get` to resolve user profiles during activity enrichment [Confirmed].

#### Inbound Dependencies
* **`building`**: 6 touchpoints [Confirmed]. Calls `OSKAccessControlDeviceConfigController` to save/delete configs, and `OSKAccessControlDeviceController` to assign/unassign building doors [Confirmed].
* **`core`**: 4 touchpoints [Confirmed]. Calls `OSKAccessControlDeviceAccessCommandsController`, `OSKAccessControlDeviceStateController`, and `OSKAccessControlDeviceSystemLogsController` to persist incoming IoT/PubSub payloads [Confirmed]. Also calls `OSKActivityEnrichmentService` to process raw events [Confirmed].
* **`call`**: 2 touchpoints [Confirmed]. Calls `OSKAccessControlDeviceController.get` and `OSKActivityEnrichmentService.enrichAndValidateActivity` [Confirmed].
* **`supplier`**: 2 touchpoints [Confirmed]. Imports `OSKActivityEnrichmentService` types for staff activity aggregation [Confirmed].
* **`user`**: 3 touchpoints [Confirmed]. Imports `OSKActivityEnrichmentService` types and references `OSKAccessControlDeviceTokenPayload` [Confirmed].
* **`admin`**: 1 touchpoint [Confirmed]. Calls `OSKAccessControlDeviceController` to list and update devices [Confirmed].
* **`organization`**: 1 touchpoint [Confirmed]. Calls `OSKAccessControlDeviceConfigController` to fetch and save configurations [Confirmed].

### 11. External Hooks

#### _module_root

### Pub/Sub Integrations
- **`accessControlDeviceConfigs`**: Confirmed Pub/Sub topic used to publish public key updates to edge devices [Confirmed; `external_hook|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device_public_keys.controller.ts|accessControlDeviceConfigs|#1`].
- **Dynamic Config Topic**: Publishes configuration updates to a dynamically resolved topic name (e.g., based on device type or environment) [Confirmed; `external_hook|access_control_device|functions/src/modules/access_control_device/controllers/access_control_device_config.controller.ts|topicName|#1`].

### External APIs & SDKs
- **Node IoT API**: Communicates with an external IoT gateway via HTTP POST/DELETE requests to register and unregister devices [Confirmed; `call_expression|access_control_device|functions/src/modules/access_control_device/api/node-iot-api/services/node_iot_api.service.ts|axios.default.post|post|url,body|#1`].
- **GCP IAM Credentials API**: Uses `@google-cloud/iam-credentials` to sign JWTs on behalf of a service account for secure communication with the IoT API [Confirmed; `imports_dependency|access_control_device|functions/src/modules/access_control_device/api/node-iot-api/services/node_iot_api.service.ts|@google-cloud/iam-credentials|#1`].

### 12. Architectural Observations

* **Separation of Concerns**: The module exhibits a clean separation between hardware-facing IoT communication (delegated to `OSKNodeIoTAPIService` and Pub/Sub) and business-logic enrichment (handled by `OSKActivityEnrichmentService`) [Confirmed].
* **Orchestration & Coupling**: It acts as a highly coupled utility hub. It does not operate in isolation; instead, it is heavily imported by almost every major domain module (`building`, `user`, `supplier`, `call`, `admin`) to resolve device states or enrich activity logs [Confirmed].
* **Data Enrichment Pattern**: Raw hardware events are not written directly to Firestore. Instead, the `OSKActivityEnrichmentService` acts as an intermediary, pulling context from `building`, `door`, and `user` modules to transform raw telemetry into enriched, business-meaningful activity documents [Confirmed].

### 13. Risks & Open Questions

**Cross-cutting risks:**

* **Undocumented RBAC Permission**: The code enforces `accessControlDevice.publicKey.maintain` during public key registration, but this string is completely missing from `rbac-roles.json` [Confirmed]. This creates a risk of broken access control or administrative bypass if roles are provisioned solely based on the JSON schema.
* **Asymmetric Authorization Enforcement**: While public key registration enforces a specific permission check, other highly sensitive operations (such as device registration, unassigning building doors, or deleting configurations) do not show explicit RBAC checks within this module's extracts, indicating a reliance on upstream caller validation (e.g., in `building` or `admin` modules) [Inferred].
* **Dynamic Topic Resolution**: The mechanism for resolving dynamic Pub/Sub topic names via `this.getTopicName()` in `OSKAccessControlDeviceConfigController` is unresolved in the evidence, posing a risk of message misrouting if configuration topics are misaligned [Inferred].
* **IoT API Configuration**: The base URL for the external Node IoT API is resolved dynamically via `this.url(path)` in `OSKNodeIoTAPIService` with no clear fallback or environment configuration evidenced in this pack [Confirmed].

**Per-capability open questions:**

#### _module_root

- **IoT API Base URL**: The base URL for the external Node IoT API is resolved dynamically via `this.url(path)` in `OSKNodeIoTAPIService` [Confirmed; `call_expression|access_control_device|functions/src/modules/access_control_device/api/node-iot-api/services/node_iot_api.service.ts|this.url|post|path|#1`]. The configuration source (e.g., environment variables) for this base URL is not evidenced in this pack.
- **Undocumented Permission**: Why is `accessControlDevice.publicKey.maintain` used for key management instead of a standard RBAC permission like `v1.admin.accessControlDevice.edit`?
- **Dynamic Config Topic Resolution**: What is the exact naming convention and resolution logic for the dynamic topic name returned by `this.getTopicName()` in `OSKAccessControlDeviceConfigController`?

### 14. Evidence References

Evidence references are preserved inline within each capability's assembled sections above (Sections 3, 4, 6, 7-8, 9, and 11) -- this section is generated deterministically, not re-derived by an LLM, to avoid fabricating a citation index from content the connective-tissue step was never given.