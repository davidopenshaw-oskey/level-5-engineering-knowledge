# P1 Build Task List — `ios-oskey-dev` family (6 repos)

Real, ordered build tasks for this family's own `pipeline/ios-oskey-dev/phase-01-ast-extraction/` (and 5 sibling `pipeline/<leaf-package>/phase-01-ast-extraction/` trees), following the same convention as `android-intercom-oskey-io/02-p1-build-tasklist.md` — struck through with a resolution note once done, not a running brainstorm. Grounded in `08-swiftsyntax-vs-npm-treesitter-bounded-test-2026-09-09.md` and `09-task1-decision-swiftsyntax-2026-09-09.md` — read both before starting any task below.

**Status:** Task 1 (tool choice) DECIDED 2026-09-09, independently confirmed 2026-09-10 — SwiftSyntax, not `tree-sitter-swift`. Tasks 1-9 DONE (see each task's own entry below) — the full `00`-`07` pipeline is real, verified, and runs end-to-end for `swift-ble-kit-oskey-dev`, with Tasks 4/5/9 also independently re-verified against 3 more repos (`swift-webrtc-kit-oskey-io`, `swift-ui-kit-oskey-dev`, `swift-cloud-kit-oskey-dev`) as part of Task 10's own real progress. `swift-ai-kit-oskey-io` removed from scope 2026-09-10 (real, unresolved file-count discrepancy, see Task 10's own entry). Real structural difference from Kotlin's list: this is **one family of repos**, not one repo — Tasks 2-9 below were built scoped to a single pilot repo first (`swift-ble-kit-oskey-dev`, per `01-multi-repo-architecture-methodology-fit-2026-09-09.md`'s "leaf packages first, smallest first" sequencing), then the shared `pipeline/swift/` restructuring (`12-...md`) meant re-running them against the rest of the family was mostly a validation exercise, not new build work.

**PATH UPDATE, 2026-09-10 — read before touching any script referenced below**: every `pipeline/swift-ble-kit-oskey-dev/...` path in Tasks 2-8's own entries below is now `pipeline/swift/...` — the pipeline was restructured to one shared folder for all 6 Swift repos (not rewritten in place below, per this project's own "mark superseded, don't rewrite history" convention; the task entries below accurately describe what was true when each was built). Full reasoning, what changed, and a real cross-repo finding (a Firestore chain-walking risk relevant to the not-yet-built `swift-cloud-kit-oskey-dev`) in `12-pipeline-restructure-shared-swift-scripts-2026-09-10.md`.

---

## Tasks

1. ~~**Decide the real Swift extraction tool.**~~ **DECIDED 2026-09-09, confirmed 2026-09-10 — see `09-task1-decision-swiftsyntax-2026-09-09.md`.** SwiftSyntax (100% clean parse across all 821 real files in the family) over `tree-sitter-swift` (a real, root-caused, *cascading* — not localized — parse-corruption bug on `#if`/`#else`/`#endif` conditional-compilation blocks, present in ordinary real code, not an edge case). A custom import-aware symbol table is still needed on top of SwiftSyntax for call-graph/cross-module resolution, per `08-...md` §3 — `resolved_via_import` / `resolved_via_same_target` / `unresolved`, plus an explicit "local/same-module declarations shadow same-named imports" rule (the real `OSKBKCentralManagerHostViewModifier` cross-target shadow case found in `08-...md` §5c).

2. ~~**Update `config/repos.json`'s real `astTool`/`astToolVersion`/`callResolutionStrategy`/`astErrorTolerancePercent` fields for `swift-ble-kit-oskey-dev`**~~ **DONE 2026-09-10.** `astTool: "SwiftSyntax"`; `astToolVersion` records the *pinning strategy* (match the host's installed toolchain, re-derive via `swift --version`, not a fixed constant to copy forward), not just `603.0.2`; `callResolutionStrategy: "import-and-target-aware-symbol-table"` with the same-target/import/shadow-rule model from Task 1 spelled out in its `_note`, including the one real cross-target case that touches this repo (`OSKBKCentralManagerHostViewModifier`, shadowed inside `ios-oskey-dev`); `astErrorTolerancePercent: 0`, the real measured rate for this repo specifically (confirmed twice — single-repo and family-wide tests both found 0 errors here), not copied from Kotlin's `5`. Deliberately scoped to this repo only — the other 5 repos' `astTool` fields stay `null` until Tasks 10-11.

3. ~~**Build the SwiftSyntax extraction package + child-process wiring**~~ **DONE 2026-09-10, real and verified.** SPM package built at `pipeline/swift-ble-kit-oskey-dev/phase-01-ast-extraction/swift-extractor/` (`Package.swift` pinned `exact: "603.0.2"`, with an explicit in-file note that this pin must be re-derived per-machine, not copied — same caveat as the `config/repos.json` note from Task 2). `swift build -c release` succeeds cleanly. Copied `_shared/run-utils.ts` from `android-intercom-oskey-io` verbatim (per that file's own scope note that per-repo duplication is intentional, no cross-repo shared runtime), header comment updated to credit the real chain of custody rather than the stale `angular-app-oskey-io` reference. Round-trip proven for real via a standalone proof script (`verify-task3-wiring.ts`, deliberately NOT one of the numbered `00`-`07` pipeline scripts — Node's `child_process.execFileSync` invokes the compiled binary against the real cloned repo, reads the JSON back, and fail-closes if the result doesn't match this repo's known-real baseline: exactly 32 files, 0 diagnostics, matching `08-...md`'s measured numbers exactly). `.gitignore` updated with `.build/`/`.swiftpm/` (confirmed via `git check-ignore` the build artifacts are correctly excluded, source files are not). `verify-task3-wiring.ts` was temporary scaffolding, not a permanent pipeline script — **deleted 2026-09-10** once Task 5's real `01-extract-ast-evidence.ts` exercised the same subprocess call for real (not Task 4, which never touches the Swift binary — `00-scan-repo.ts`'s job is the file inventory, not parsing), per this project's own discipline against leaving diagnostic scripts lying around once their finding is written up.

4. ~~**Port the `00-scan-repo.ts` equivalent**~~ **DONE 2026-09-10, real and verified.** Closer to node-iot-api-oskey-io's own single-hardcoded-module precedent than Kotlin's Gradle multi-module version — `HARDCODED_MODULE_NAME = "OSKBluetoothLEKit"`, no submodule BFS (single target, 30 real files, well under the threshold that would warrant a split, per Kotlin's own precedent for its small library modules). File collection deliberately scoped to `modulesRoot` (`Sources/`) only, excluding `Tests/` — real declaration extraction is Task 5's job, not this script's. Run twice for real (once directly, once via a new `cloud:swift-ble-kit-00-scan-repo` npm script) against the real `https://github.com/oskey-io/swift-ble-kit-oskey-dev.git`: correctly clones, checks out the exact pinned commit (`f8cdf199...`, verified matching), finds exactly 30 real `.swift` files under `Sources/`, writes `run-context.json`/`facts/modules.json`/`facts/files.json`/`run-notifications.json`/`latest-repo-manifest.json` in the same shape as the other repos' pipelines.

5. ~~**Port the `01-extract-ast-evidence.ts` equivalent**~~ **DONE 2026-09-10, real and verified.** `swift-extractor` (Task 3's package) extended from a diagnostics-only wiring proof to real per-file declaration/call facts (classes, structs, enums, protocols, extensions, functions, properties, imports, calls) via a `SyntaxVisitor` tracking a type/function context stack. Resolution logic (import-aware/same-target symbol table) built in TypeScript, not Swift — a deliberate split, since resolution is pure name-matching with nothing SwiftSyntax-specific about it, keeping the Swift-toolchain dependency confined to parsing alone. Two real bugs found and fixed before trusting the output, same discipline as every prior tool-comparison step in this family: (1) `rootIdentifier` for a call-then-navigation chain like `Logger().info(...)` came out as `"Logger()"` not `"Logger"` — same class of bug Kotlin's own resolver already guards against, fixed by stripping from the first `(` too; (2) the declaration table was first built from only *top-level* (`parentType === null`) functions, producing 0 real `resolved_via_same_target` matches out of 433 real calls — almost every real call here is to a sibling *method*, not a top-level function; fixed by indexing every declaration regardless of nesting depth, matching Kotlin's own actual (recursive) resolver behavior exactly. Final, verified counts on the real repo (updated 2026-09-10 after the init/deinit fix below): 8 classes, 9 structs, 10 enums, 2 protocols, 13 extensions, 91 functions (71 real `func`s + 16 `init` + 4 `deinit`, see `11-cross-pipeline-lessons-checked-against-swift-2026-09-10.md`), 141 properties, 56 imports, 433 calls (71 `resolved_via_same_target`, 28 `self_reference`, 334 `unresolved` — the last figure honestly includes real external-framework calls like `CoreBluetooth`/`Combine`/`os.Logger`, not distinguished from a real gap in this pass, since `resolved_via_import` has nothing to match against yet — see Task 11). **Real, valuable side-finding**: cross-checking these numbers against `08-...md`'s own declaration counts surfaced that doc's `extension: 11` "ground truth" was itself wrong (real count 13, confirmed twice independently) — corrected in that doc directly rather than left standing; does not change the Task 1 decision, which turned on cascading parse errors, not this count.

6. ~~**Design domain-specific "wire format" fact types** for BLE~~ **DONE 2026-09-10, real and verified.** Direct Swift analog of Kotlin's `ast-ble-gatt-constants.json`, detected structurally via `CBUUID(string: "...")` calls (Swift/CoreBluetooth's real analog to Kotlin's `UUID.fromString(...)`). Required extending `swift-extractor`'s `CallFact` with a real `arguments` field (raw source text per argument, mirroring Kotlin's own convention) — a generically useful addition, not a one-off hack. **Real cross-repo finding, exactly the kind this task was written to look for**: 5 of this repo's 6 real UUID constants are **byte-for-byte identical** to Android's own `OSKUUIDTable.kt` values (`advertizingServiceUUID`, `unlockServiceUUID`, `unlockCentralCmdCharUUID`, `unlockPeripheralCmdCharUUID`, `doorStatusCharUUID`) — direct, concrete evidence both platforms implement the same physical BLE lock protocol, not independent reimplementations. The 6th (`sesameAdvertizingServiceUUID`) has no Android equivalent — a real, Swift-only extension (a different lock hardware vendor), not a gap. `kind` classification uses this repo's own real, observed naming convention (contains "Service"/"Char" as a camelCase suffix) rather than Kotlin's `GATT_SERVICE_*`/`GATT_CHAR_*` prefix convention, which this repo's source doesn't use at all.

7. ~~**Verify real extraction coverage**~~ **DONE 2026-09-10, real and thorough.** Classes/structs/enums/protocols/extensions were already exactly reconciled against `grep` during Task 5's own build (8/9/10/2/13). This pass covered the rest: `imports` (56) and `functions` (71) matched an independent `grep` count **exactly**, no investigation needed. `properties` initially showed a 141-vs-142 mismatch against a naive `grep` count — investigated every one of the 4 discrepant lines individually rather than assuming either number was right (this project's own standard discipline, the same one that caught Kotlin's real constructor-promoted-property gap). Real result: **the extractor was correct in all 4 cases, the naive `grep` regex was wrong in all 4** — 3 were `guard let x = ..., let y = ...` multi-condition optional-binding clauses (not property declarations at all, correctly excluded by SwiftSyntax's real `VariableDeclSyntax` vs. binding-condition distinction) and 1 was a line sitting inside a `/* ... */` commented-out dead-code block that a text-based regex has no way to know is inactive. Separately, `grep`'s narrower modifier list undercounted 3 real property-wrapper-attributed properties (`@Published var`, `@State private var`, `@StateObject private var`) that the extractor correctly captured. **Calls (433)** were not independently full-counted (not a `grep`-friendly construct, consistent with this project's own established precedent) but spot-checked directly: confirmed zero calls were extracted from that same commented-out block (comment-handling is consistent across fact kinds), and checked for labeled/multiple trailing closures (the exact construct that broke `tree-sitter-swift` in a sibling repo, per `08-...md` §5a) — none exist in this repo's own real source, so that specific risk class is honestly not exercised here, not silently assumed safe.

8. ~~**Design what gets embedded, per fact kind**~~ **DONE 2026-09-10, real and verified.** Extended the shared `sync-facts.ts`'s `descriptionFor()`, mirroring Kotlin's own Task 7 addition. Real work along the way, not a mechanical port:
   - **Found and fixed a real go-back-and-fix gap in Task 5's own extractor**: it never captured enum *cases* at all — surfaced only because this task asked "what's worth surfacing per fact kind" and the honest answer for Swift's own real enums (`case unlock = 0x02`, `case locked = 0x00`) was "the exact closed-set-value pattern this project has repeatedly found to be its single highest-value fact kind" (USB wire constants, Kotlin enum members — same lesson, third time). Extended `swift-extractor` (schema 2.2.0) to capture real case names/raw-values/associated-value types; verified against real data (9 of this repo's 10 enums have real cases, the 1 without — `enum CRC32` — confirmed by reading its source to be a genuine zero-case namespacing idiom, not a bug).
   - Added Swift-specific description branches: enum cases (kept deliberately separate from Kotlin's `members` shape — a Swift case's `rawValue` and `associatedValues` are two genuinely different things Kotlin's `constructorArgs` doesn't distinguish), a list-based inheritance/conformance doc for class/struct/enum/protocol (Swift's inheritance clause is a list — `class Foo: NSObject, Codable`— unlike TS/Kotlin's single-string `extendsClass`, which would have silently dropped every conformance past the first), and a new `extension_declaration` branch (no TS/Kotlin analog at all).
   - **Real cross-language field-name fix**: renamed `callerType` to `callerClass` at the point this script emits `ast-calls.json` (not upstream in the Swift extractor, which keeps the more accurate name internally) — the existing shared `callExpressionDoc`/`kotlinCallResolutionDoc` branches read `fact.callerClass`, and without this rename Swift's own calls would have silently gotten zero enrichment from code that already existed and already worked.
   - **Confirmed working for free, zero changes needed**: the existing `bleGattDoc` branch already matches Swift's `ast-ble-gatt-constants.json` field names exactly (`uuidValue`, `kind`) — a direct payoff of deliberately mirroring Kotlin's shape in Task 6, not a coincidence.
   - All five new/changed branches verified directly against real fact shapes (not just "it compiles") — every one produced correct, real rendered text.
   - **Real, deliberately deferred, not silently ignored**: the existing `kotlinCallArgumentsDoc` branch's "simple string arg" filter requires an argument to be a bare quoted literal; Swift's labeled-argument convention (`string: "..."`) never matches that shape, so labeled Swift call arguments get zero enrichment there today. Not fixed in this pass — the one real case that would have benefited (the BLE UUID constants) is already fully covered by the dedicated `bleGattDoc` branch, so this is a real but low-value gap, not a silent one.

9. ~~**Downstream scripts** (`02`-`07` equivalents)~~ **DONE 2026-09-10, all six built and verified against all 4 already-onboarded repos, not just the pilot** (built once in the shared `pipeline/swift/`, per the restructuring). `05`, `06`, `07` ported near-mechanically once `02`/`04` were real — confirmed generic exactly as `08-...md` §4 predicted, and ran cleanly across all 4 repos producing correctly HONEST empty results for cross-module/intra-module coupling (real, expected: no repo has real submodule structure yet, and Swift's cross-target import resolution isn't built — not a bug, matching this file's own header comments on both new scripts). Full end-to-end chain verified via a new `npm run pipeline:swift-ble-kit` wrapper (mirroring the `pipeline:android-intercom` convention), re-cloning and running `00` through `07` in one real command. Real value from `02`/`04` specifically — building canonical fact-typing and call-graph eligibility classification for real surfaced four real, root-cause bugs in the extractor itself, found via genuine frequency measurement, not guessed:

    - **`02-build-module-evidence.ts`**: ported cleanly, adapted for Swift's real fact vocabulary (`class_declaration`/`struct_declaration`/`enum_declaration`/`protocol_declaration`/`extension_declaration`/`function_declaration`/`model_property`/`call_expression`/`imports_dependency`/`ble_gatt_constant` — no Kotlin `objects`/`sealedHierarchies`/`usbWireConstants`/`webrtcSignalingTouchpoints`, since none exist for this family). One real adaptation, not assumed: Swift's manifest has no top-level `errors: {file, recordCount}` object the way Kotlin's does — checked directly against the real manifest this pipeline actually writes rather than copying Kotlin's validation blindly. Verified end-to-end against real data: `descriptionFor()` correctly rendered real enum cases, BLE constants, extension conformance, and a resolved call — the full `01`→`02`→enrichment chain confirmed working on real output, not synthetic test facts.
    - **`03-build-benchmark.ts`**: ported cleanly, same `errors`-field adaptation as `02`. Verified: 799 real facts for the pilot repo reconciled exactly (30 files + 56 imports + 42 type declarations + 91 functions + 141 properties + 433 calls + 6 BLE constants).
    - **`04-build-resolved-graph.ts`**: real, deliberate re-design, not a blind port. `NOISY_CALL_ROOTS` rebuilt from a genuine frequency measurement across all 4 repos' real `ast-calls.json` (6,378 calls), not guessed — same discipline as Kotlin's own list, only including names independently confirmed external (SwiftUI/Foundation/Combine/Firebase-SDK), never a lowercase name that could be a real in-repo reference. Added a real, honestly-named THIRD bucket with no Kotlin/TS analog: `implicitMemberExpressionCalls` — Swift's own leading-dot shorthand (`.missingService(...)`, inferring an enum/type from context) is structurally unresolvable by a syntax-only tool, the direct Swift analog of Kotlin's own indirect-enum-value-through-aliasing gap (`06-ts-pipeline-lessons-reflagged-for-tree-sitter-2026-09-08.md` item 1) — confirmed real and non-trivial (261 real occurrences across the 4 repos). Verified math reconciles exactly across all 4 repos (excluded + implicit + eligible = total, every time).

    **Four real bugs found and fixed in the Swift extractor itself while building this task, in the order found** (schema 2.4.0 → 3.0.0), all via genuine measurement against real data, not theorized:
    1. Generic-class constructor calls with an explicit type argument (`OSKCKFunctionService<OSKCKNoResponse>(...)`, a real declared class in `swift-cloud-kit-oskey-dev`) kept their own `<...>` suffix in the computed root, never matching the bare-name declaration key — silently failing resolution for every such call site. Fixed by stripping the generic-argument suffix too. Real, measured impact: `swift-cloud-kit-oskey-dev`'s same-target resolution went from 152 → 188.
    2. Swift's `.split(separator:)` omits empty subsequences by default, so a genuine implicit-member-expression call (`.signUp(...)`) had its leading `.` silently dropped, colliding with an unrelated in-repo declaration of the same bare name and producing a **confirmed false-positive resolution** (`.signUp`/`.enrollMFA` in `swift-cloud-kit-oskey-dev` "resolved" only by name coincidence). Fixed by detecting a leading `.` explicitly and giving it an honest empty root instead.
    3. **Most consequential**: the whole string-splitting approach to root-identifier computation broke down for any call chain deeper than ~2 hops — `node.calledExpression.trimmedDescription` for the outermost call in a chain captures the *entire* preceding chain as raw text (confirmed real: `Text("...").font(...).foregroundColor`'s callee text is the whole multi-line blob, not just its immediate base), and splitting on the first "." finds whatever token happens to precede it, not the real structural root. **Replaced entirely** with a real recursive walk of the actual syntax tree (`rootIdentifierOf(_:)`, handling `MemberAccessExprSyntax`/`FunctionCallExprSyntax`/`GenericSpecializationExprSyntax`/optional-chaining/force-unwrap), correct for a chain of any depth.
    4. **Real, measured, genuine ambiguity found while verifying fix #3, not a bug**: `swift-ui-kit-oskey-dev` has its own real `enum Text` nested inside `Color` (`Color.swift:83`) — a real, direct collision with SwiftUI's own `Text` view. The flat, name-only same-target resolver (a known, already-accepted limitation recorded back in Task 5's own build) has no concept of nested-type qualification scoping, so all 122 real `Text(...)`-rooted calls — overwhelmingly real SwiftUI usage — get attributed to the local nested enum instead. **Not fixed** — proper resolution would need real lexical-scope-aware analysis, the same class of semantic-analysis gap this project has consistently accepted rather than attempted to solve (matching Kotlin's own accepted call-graph-resolution limits). Recorded here as a real, concrete, measured instance of an already-flagged risk class, not a new one.

10. **Propagate the now-proven pipeline to the remaining leaf packages** (`swift-cloud-kit-oskey-dev`, `swift-webrtc-kit-oskey-io`, `swift-ui-kit-oskey-dev`) — expected mechanical once Tasks 2-9 are proven against the pilot repo, since the extractor/resolver built in Tasks 3/5 should be repo-agnostic by construction (config-driven, like the TS/Kotlin pipelines already are).

    **`swift-webrtc-kit-oskey-io`: DONE 2026-09-10, real and verified — genuinely mechanical, exactly as predicted.** Config filled in (Task 2 equivalent), ran `00`→`01` for real via the shared `pipeline/swift/` scripts. Real numbers: 87 real files in scope (`Sources/` only), 20 classes/53 structs/15 enums/14 protocols/23 extensions (all exactly matching independent `grep`), 290 functions (24 init + 11 deinit, confirming the init/deinit fix generalizes correctly to a second, more complex repo), 476 properties, 143 imports, 1366 calls (163 same-target, 1142 unresolved — plausible given real dependencies on `socket.io-client-swift` and a prebuilt `WebRTC.xcframework`), 0 parse errors, 0 BLE GATT constants (correctly empty — no `CBUUID` usage here, proving the domain-specific detector degrades harmlessly on a repo with none). One apparent file-count discrepancy investigated and fully explained, not left open: the config's own `_note` claimed 89 files; `00-scan-repo.ts` correctly found 87 (`Sources/`-only). Reconciled exactly: 87 (`Sources/`) + 1 (`Tests/`) + 1 (`Package.swift` at repo root) = 89 — the extractor's own whole-clone invocation log line reports the wider number, `00`'s scoped count is the correct one for extraction purposes, same reconciliation already established for `swift-ble-kit-oskey-dev`'s own 32-vs-30 case. A second, smaller discrepancy (grep found 23 real `init`s, extractor found 24) also investigated: grep's own regex modifier list didn't include `override` (`override private init()`), a verification-script gap, not an extraction bug — the extractor was right.

    **`swift-ui-kit-oskey-dev`: DONE 2026-09-10, real and verified — also genuinely mechanical.** Config filled in, ran `00`→`01` for real. Real numbers: 91 real files in scope (`Sources/` only), 15 classes/74 structs/22 enums/4 protocols/53 extensions (all exactly matching independent `grep`), 148 functions (48 init, 0 deinit, exact match to `grep` once the verification regex's own `override` gap from the previous repo was fixed), 534 properties, 118 imports, 1486 calls (319 same-target, 1153 unresolved), 0 parse errors, 0 BLE GATT constants (correctly empty). File-count discrepancy (91 found vs. 103 claimed in config) investigated and fully reconciled, not a gap: this repo has a real, separate `Sample/` demo app (10 real Swift files) plus `Tests/` (1) plus `Package.swift` (1) — 91 + 10 + 1 + 1 = 103 exactly. Specifically checked that `Sample/`'s own `ContentView` (the real cross-repo name collision found in `08-...md` §5c) did **not** leak into the real facts — confirmed zero `Sample/`-sourced records anywhere in `ast-structs.json`, scoping is clean.

    **`swift-cloud-kit-oskey-dev`: DONE 2026-09-10, real and verified — the last of the 4 repos in this validation pass.** Config filled in, ran `00`→`01` for real. Real numbers: 127 real files in scope (`Sources/` only), 30 classes/128 structs/42 enums/4 protocols/15 extensions (all exactly matching independent `grep`), 255 functions (93 init, 0 deinit, exact match), 853 properties, 177 imports, 979 calls (152 same-target, 781 unresolved), 0 parse errors, 0 BLE GATT constants (correctly empty — no `CBUUID` usage in a Firestore-focused repo).

    **File-count discrepancy, different in character from the previous two — flagged, not fully reconciled.** Config's own `_note` and `swift-cloud-kit-oskey-dev/00-repo-snapshot-2026-09-09.md` both claimed 111 real Swift files (`Sources/` + `Tests/` combined); this scan found 127 in `Sources/` alone (128 with `Tests/`). Unlike the two prior repos' discrepancies, this doesn't reconcile against a known excluded directory (no `Sample/`-equivalent exists here) — the most likely explanation is the original manual count from 2026-09-09 simply undercounted (matching the pattern already found twice this session: doc `08`'s own `extension: 11` vs. real `13`), since the commit is pinned by exact SHA and should be deterministic. **Not chased further to full root cause** — real file-by-file diffing against whatever the original inspection actually counted was not done.

    **Real finding, FIXED 2026-09-10, immediately after being reported.** 121 of 979 calls (12.4%) in this repo were `#if canImport(...)`-style compiler-directive checks (`canImport(FirebaseFirestore)`, etc.), not real function calls — syntactically identical to a real call, so the generic call extraction swept them up. First checked whether this meant real import data was being lost (a much more serious concern) — verified directly it was not: every real `import` statement guarded by such an `#if` block was already correctly captured in `ast-imports.json`, at the right lines. The bug was pure mis-categorization noise in `calls`, never data loss.

    Fixed structurally in `swift-extractor` (schema 2.4.0), not by guessing at compiler-directive names: added a `visit(_ node: IfConfigClauseSyntax)` override that walks every child of an `#if` clause EXCEPT its own `condition` (matched by real node identity via `.id`, not text) — the guarded code itself (real imports, declarations, calls) is still visited normally. Chosen over a downstream `02`-`07` fix specifically because those scripts never see the syntax tree at all, only `01`'s already-written JSON — patching there would leave the wrong data sitting in the authoritative raw-evidence file for every other consumer to independently rediscover.

    **Verified across all 4 repos already run, not just the one that surfaced it**: `swift-ble-kit-oskey-dev` and `swift-webrtc-kit-oskey-io` showed zero change (433/1366 calls, confirming they have no real `#if`-condition-as-call instances to begin with); `swift-ui-kit-oskey-dev` dropped 1486→1423 (a real, previously-hidden 63-call chunk of the same noise, via `#if !targetEnvironment(simulator)` and others); `swift-cloud-kit-oskey-dev` dropped 979→857. Every other fact count (classes/structs/enums/protocols/extensions/functions/properties/imports) stayed byte-identical across all 4 re-runs — confirmed only `calls` was ever affected. Directly confirmed zero pseudo-calls (`canImport`/`os`/`arch`/`swift`/`compiler`/`targetEnvironment`/`hasAttribute`) remain in any of the 4 repos' `ast-calls.json` after the fix.

    **Confirmed empirically, not just reasoned about**: the real Firestore chain-walking risk flagged in `12-...md` (the TS pipeline's own one-hop-only bug that cost it 17/18 real cases) does not affect this repo's *generic* call extraction — checked directly against `OSKCKFirestoreService.swift`'s real code: `documentPath.documentRef.getDocument`, `collectionPath.collectionRef.getDocuments`, and other real terminal Firestore operations are all correctly captured as their own call facts regardless of chain depth. The risk remains real and open specifically for a *future* Firestore-specific domain-fact detector (this repo's own Task 6 analog), not for the extraction already built.

    **`swift-ai-kit-oskey-io` REMOVED from pipeline scope, 2026-09-10 — not going to be included for now.** Real, concrete reason found while validating the pipeline restructuring (`12-...md`) against this repo for real: `00-scan-repo.ts` found only **2** real Swift files (`OSKAIKit.swift`, `UIImage+Preparation.swift`), not the **4** this family's own config `_note` and `04-task1-pre-design-checklist-2026-09-09.md` have claimed since 2026-09-09 — a real, unexplained discrepancy between long-standing documentation and what the pinned commit (`cee618a8`) actually contains, not yet root-caused (not chased further per direct instruction to stop). Config entry removed entirely from `config/repos.json` (not left disabled/null — a clean removal, since this repo isn't currently in scope, not merely paused); all real artifacts the scan run wrote (`output/clones/swift-ai-kit-oskey-io/`, `output/swift-ai-kit-oskey-io/`, `output/runs/swift-ai-kit-oskey-io/`) were deleted, not left lying around. **Revisit later**: re-check the real file count against the pinned commit before re-adding — this may be a stale `_note` (written before something changed upstream) or a real repo-state discrepancy worth understanding before trusting anything else in that config entry's other notes.

11. ~~**`ios-oskey-dev` itself (the app) — sequenced last, real reasons why, not just "it's the entry point so do it last" convention.**~~ **(a) and (b) DONE 2026-09-10, real and verified — see `14-task11-pbxproj-structure-bounded-test-2026-09-10.md` and `15-task11-built-verified-and-duplicate-names-resolved-2026-09-10.md`.** (a) Real `.pbxproj`-aware `00-scan-repo.ts` built at `pipeline/ios-oskey-dev/phase-01-ast-extraction/00-scan-repo.ts` (uses the real `xcode` npm parser + a hand-built group-parent-chain path resolver, since neither the library nor a bare-basename search correctly handles Apple's newer per-target "file system synchronized group" feature or this repo's 3 real same-named folders). Real, dynamic target discovery (3 found: `iOS App`, `OSKDoorUnlockActivityExtension`, `OSKEYTests`) with membership resolved from the union of explicit `PBXSourcesBuildPhase` file lists and real recursive on-disk folder walks. **Real, user-confirmed scope decision**: 13 real `.swift` files that belong to zero real Xcode targets (confirmed absent from `project.pbxproj` entirely, genuine dead code) are skipped entirely, not tagged. Full `00`→`07` chain run for real against real data: 469 files in scope, 0 parse errors across all 482 real files (resolves the toolchain-gap risk flagged in `02-real-repo-inspection-findings-2026-09-09.md` §8), 25,458 total facts, all per-module/per-stage counts reconciling exactly. `config/repos.json` filled in for real (`astTool`/`astToolVersion`/`callResolutionStrategy`/`astErrorTolerancePercent: 0`, all measured, not copied). (b) The 3 real same-target duplicate names from `08-...md` §5c/5d, now explained via real target-membership data: `OSKStreetAddressPickerViewModel` was never a real collision (one of its two `grep` hits is one of the 13 real orphaned/dead files); `OSKUserIdKey` and `Provider` are real same-target duplicates but genuine Swift nested-type namespacing (each pair nested inside a different enclosing extension/widget type) — the same already-accepted flat-resolver nested-scope limitation class as `swift-ui-kit-oskey-dev`'s own `enum Text` case, not a new problem, not fixed (consistent with prior precedent). **(c) NOT done, genuinely blocked, not an oversight**: the cross-target shadow rule (`OSKBKCentralManagerHostViewModifier`) needs Task 12 to exist first so this repo's own resolver can see across into `swift-ble-kit-oskey-dev`'s facts. **Deliberately does NOT include Task 12 below** — kept separate per direct instruction, 2026-09-10.

12. **Cross-repo declaration resolution — a new task, added 2026-09-10, deliberately kept separate from Task 11.** Real, direct motivation: `08-...md` §5c's own `OSKBKCentralManagerHostViewModifier` finding (defined in `swift-ble-kit-oskey-dev`, also locally shadowed inside `ios-oskey-dev`) — without this, `ios-oskey-dev`'s own resolver can only ever see its own local copy, with no way to know the imported one even exists. This is also the only mechanism that could ever make `resolved_via_import` actually fire (permanently 0 today across every repo in this family, per every real run this session).

    **Real, checked topology, not assumed**: verified directly against every leaf package's own real `Package.swift` — zero leaf-to-leaf dependencies exist anywhere in this family today (`swift-ble-kit-oskey-dev`: no dependencies at all; `swift-cloud-kit-oskey-dev`: Firebase iOS SDK only; `swift-webrtc-kit-oskey-io`: socket.io-client-swift + a WebRTC binary only; `swift-ui-kit-oskey-dev`: PopupView/PhoneNumberKit/SDWebImageSwiftUI only — all third-party, none each other). Today's real shape is a pure hub-and-spoke: only `ios-oskey-dev` imports siblings; no leaf package imports another. **CONFIRMED with the real iOS dev team, 2026-09-10**: this is a deliberate architectural boundary, not coincidental — no direct traffic between sub-repos, all cross-capability coordination routed through the parent app. Real, authoritative answer, not inferred from the dependency graph alone. Doesn't remove the task (the mechanism still needs to exist for `ios-oskey-dev`'s own real hub-side resolution — see `OSKBKCentralManagerHostViewModifier` above) but does confirm the leaf-to-leaf direction is genuinely, intentionally out of scope, not just unexercised by coincidence — the reactive design below already costs nothing for that direction regardless, so this confirmation changes confidence, not the design.

    **Design decision, made explicitly to avoid two different anti-patterns raised and discussed 2026-09-10**: NOT a hardcoded hub-and-spoke assumption (today's real shape could change on a future merge, matching this session's own "always dynamic, never hardcoded" principle — see `feedback_dynamic_recursive_never_hardcoded` memory), and NOT a precomputed, speculative N×N linking system built ahead of any real need (a genuine YAGNI/premature-generality risk, correctly flagged before committing to this task). The real, minimal, direction-agnostic design: each repo's own `01-extract-ast-evidence.ts` run checks **its own real, current `import` statements** at scan time, and for each one, checks whether an already-extracted sibling repo's own declaration table happens to exist on disk (tagged by source repo) — using it if present, doing nothing extra if not. Costs nothing for a repo whose real imports never name a sibling (today, that's 4 of 5 repos); picks up a real cross-package reference automatically the moment one exists, without needing to be redesigned when it does. Not yet built.

## Explicitly not in this task list

- Phase 2 (LLM synthesis/PRD generation) for any Swift repo — downstream of P1 being real for at least one repo, not scoped here.
- Automating or CI-triggering this pipeline — deliberately deferred per `05-future-trigger-driven-extraction-concurrency-risk-2026-09-09.md`; this task list assumes manual-trigger runs on a real developer machine that already has a Swift toolchain installed.
- Re-litigating Task 1's tool choice — decided, independently confirmed, closed. A future session should update `09-...md` directly if new evidence genuinely warrants reopening it, not silently work around the decision here.
