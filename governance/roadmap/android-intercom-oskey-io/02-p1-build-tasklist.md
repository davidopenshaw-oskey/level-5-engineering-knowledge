# P1 Build Task List — `android-intercom-oskey-io`

Real, ordered build tasks for this repo's own `pipeline/android-intercom-oskey-io/phase-01-ast-extraction/`, following the same convention as `facts-serving-strategy/05-tasklist.md`/`09-p2-build-tasklist.md` — struck through with a resolution note once done, not a running brainstorm. Grounded in the real investigation in `00-phase1-ast-extraction-design.md` and the standing principles in `01-standing-principles-and-lessons-from-ts-facts-pipeline-2026-09-07.md` — read both before starting any task below.

**Status:** All 8 tasks DONE as of 2026-09-09. Task 1 (tool choice) decided 2026-09-08 — see `09-task1-decision-tree-sitter-plus-import-aware-resolver-2026-09-08.md`. Tasks 2-7 built and verified 2026-09-09. Task 8 (downstream scripts `02`-`07`) built and verified 2026-09-09 — see `15-task8-downstream-scripts-built-and-verified-2026-09-09.md`. This repo's own Phase 1 pipeline (`00` through `07`) is real, runs end-to-end via `npm run pipeline:android-intercom`, and is verified against the real repo. Phase 2 (LLM synthesis/PRD generation) is still unstarted, per Task 2's own note.

---

## Tasks

1. ~~**Decide the real Kotlin extraction tool.**~~ **DECIDED 2026-09-08 — see `09-task1-decision-tree-sitter-plus-import-aware-resolver-2026-09-08.md`.** `tree-sitter-kotlin` for declaration-level facts (95.6% clean parse rate on this repo's real 136 files, verified directly) plus a custom, import-aware project-wide symbol table for call-graph/cross-module/RBAC resolution (real name collisions rare — 2 in the whole repo — and cleanly resolvable via each file's own explicit imports). `kotlin-compiler-embeddable` and the standalone Analysis API both considered and rejected; full reasoning and the bounded real parse test it's grounded in are in `08-task1-consolidated-checklist-2026-09-08.md` and `09-...md`.

2. ~~**Design this repo's own `config/repos.json`-equivalent config shape.**~~ **DONE 2026-09-09.** Real block added to `config/repos.json` — no static `modulesRoot`/module array (per standing principle 2), a `moduleDiscovery` pointer at `settings.gradle.kts`'s real `include(...)` calls instead; `astTool`/`astToolVersion` and `callResolutionStrategy` fields recording Task 1's decision directly in the config the pipeline will actually read, not just in roadmap docs; `astErrorTolerancePercent: 5` set from the real 4.4% measured baseline (`08-...md`), not copied from the other repos' `0`; `normalizationRules.stripPrefixes: ["OSK"]` confirmed real, `classificationRules` deliberately left unset (real Task 4 work, this repo's naming style differs from Firebase/node-iot's). `branch: "develop"` is an explicit, flagged placeholder — production deploys off a tagged release, and a live check found this repo's real tag history has inconsistent naming that breaks naive tag-sorting (a legacy `release_1_0_3` tag outranks the real latest `1.6.0` under git's own version-sort) — needs a conversation with the repo's own developers before a tag-based ref can be trusted; tracked separately, not blocking.

3. ~~**Write the `00-scan-repo.ts` port.**~~ **DONE 2026-09-09 — see `10-task3-scan-repo-built-and-verified-2026-09-09.md`.** Real script built and run end-to-end multiple times against the real repo; all 11 real screens correctly discovered via the BFS-from-`NavHost`-`composable()`-calls technique, with 3 real bugs found and fixed through iterative re-running (a guarded/nested screen render, structurally-shared Hilt/`Application` files, and same-package-no-import ViewModel/UseCase/Repository chains) — full debugging history recorded since it's a real, portable lesson for any future language's own import-graph BFS. Sub-question resolved: the 4 library modules get `submodule=null` uniformly (no partitioning) per the config's own `_note_normalizationRules`-adjacent reasoning — real file counts (5-17 each) are well under node-iot's own no-split precedent.

4. ~~**Write the `01-extract-ast-evidence.ts` port**~~ **DONE (V1) 2026-09-09 — see `11-task4-extract-ast-evidence-built-and-verified-2026-09-09.md`.** Real script built, run end-to-end, every fact type spot-checked against known-correct real data. Enum members (with constructor args), sealed hierarchies (same-file scope), generic type-argument descent (heritage clauses + property initializers), and call-expression resolution (import-aware, honestly confidence-tagged) all verified working. Two real grammar-quirk bugs found and fixed (annotation attachment location, no separate `interface_declaration` node type) — see that doc for the debugging history.

5. ~~**Design and build the domain-specific "wire format" fact types**~~ **DONE 2026-09-09 — see `12-task5-wire-format-facts-built-and-verified-2026-09-09.md`.** Real, detectable via static AST alone, no runtime/config inspection needed. WebRTC event *names* turned out to already be fully covered by Task 4's generic enum extraction (no new fact type needed there) — the real new value was the call *sites* (`.on`/`.emit`), the genuine `firestore_path_touched` analog. `ast-ble-gatt-constants.json` (22 real UUIDs, classified), `ast-usb-wire-constants.json` (21 real hex constants, including real inline comments carrying otherwise-unrecoverable business meaning like "open door command"), `ast-webrtc-signaling-touchpoints.json` (14 real touch points, after fixing a real `.emit()` naming collision with Kotlin Coroutines' unrelated `Flow.emit()` — 67 unfiltered matches down to the 1 real wire-protocol call, verified by direct grep before trusting either number).

6. ~~**Verify real extraction coverage**~~ **DONE 2026-09-09 — see `13-task6-coverage-verification-2026-09-09.md`.** Classes/interfaces/enums matched an independent grep count exactly. Two apparent mismatches (objects, `@Composable`) both investigated and explained as correct behavior (anonymous object expressions; a type-position annotation), not silently dismissed. One real, significant gap found and fixed: constructor-promoted properties (`class Foo(val x: Int)`) parse as a different node type (`class_parameter`) than plain properties and were entirely missing — 406 real properties recovered (~26% increase) once fixed.

7. ~~**Design what gets embedded, per fact kind**~~ **DONE 2026-09-09 — see `14-task7-description-enrichment-built-and-verified-2026-09-09.md`.** Real branches added to the shared `pipeline/facts-postgres-index/sync-facts.ts`'s `descriptionFor()`, verified directly against real raw facts from Tasks 4-6 (manually tagged with their intended `fact.type`, since real `type`/`id` assignment is a Task 8 concern — found and scoped as part of doing this properly). All 9 real test cases produced correct, high-quality output on the first working version, including the exact `OSKAnalyticsService` call-resolution case central to Task 1's decision.

8. ~~**Downstream scripts** (`02`-`07` equivalents — module evidence, resolved graph, capability packs, cross-/intra-module coupling)~~ **DONE 2026-09-09 — see `15-task8-downstream-scripts-built-and-verified-2026-09-09.md`.** `02`/`04` were real, deliberate re-designs (no Mongo/routes/Joi/PubSub/RBAC exists for this repo; call resolution already happens in `01`, not `04`); `05`/`06`/`07` (and `03` minus its POC-scoring section) were confirmed fully generic and ported near-mechanically. Real eligibility filter built for `04` per this note's own original concern, grounded in a real measured root-identifier frequency count (not guessed) — 41 real cross-module call edges surfaced (e.g. `app -> kotlin-ble-kit-oskey-io`). Three real gaps found and fixed in `01-extract-ast-evidence.ts` before `02` could even be built (imports were never resolved to a target module, no parse-error tracking/tolerance enforcement despite it being configured, no evidence manifest) — see that doc for the full list, plus a real post-build bug (`item.path` vs `item.file` field-name mismatch) caught only by testing real output end-to-end, not by reading the code.

---

## Explicitly not in this task list

- The standalone P2/MCP-serving side for this repo's facts (Postgres sync, embedding, search) — downstream of P1 even being real, not scoped here.
- Any resolution of the `mcp-server`/`_shared` coupling question — that's Task 1 (a peer session, in progress as of 2026-09-07), independent of this list.
