# tasks & todos

1. remove hard coded paths, even from the repo, in the ast extraction
2. remove all of the hardcoded references, make it 100% dynamic
3. ensure all paths are relative for agentic running
4. governance/reference-docs (schemas, indexes, firestore rules, rbac roles) are currently manual snapshots — make these dynamically re-derived from the target repo when read in on P1
5. p2 synthesis artefact storage still needs a real answer from devops/engineering. `/output` stays gitignored (repo clones + raw facts belong there). Finished synthesis documents now write to `knowledge-corpus/<repo>/<runId>/...` (git-tracked, provisional) — but this is provisional, not the final decision, especially if this pipeline ends up running on Google's Gemini Enterprise agentic platform in CI/CD (output might belong in a cloud storage bucket instead).
6. LLM auth (Gemini via Vertex AI, Claude via Anthropic API key) is set up per-person, manually, on one machine — not yet reproducible by a teammate or in CI/CD. Needs: `gcloud` SSO + an explicit IAM grant per person for now; a service account or Workload Identity Federation for CI/CD later, once the item-5 platform question is settled.
7. P1 scripts (`00-scan-repo.ts` through `06-build-cross-module-dependency-graph.ts`) are not easy for a new developer to jump into — needs a top-level README for `phase-01-ast-extraction/` explaining the script chain, plus a short "what this does and why" summary per script.
8. `knowledge-corpus/<repo>/<runId>/...` is deliberately LLM-agnostic (no provider/model in the path — see `00-generate-module-profile.ts`'s design notes), so running a second LLM_CONFIG_KEY against the same module/run overwrites the first provider's output. Need a design decision for side-by-side LLM comparison runs (candidate: a separate `output/` side-channel per llmConfigKey, promoted manually into the corpus once a winner is picked) — deferred, not decided.
9. `maxTokens: 65536` for `claude-default` (config/llm-providers.json) was set empirically 2026-08-01 by re-testing after a real truncation bug — confirmed sufficient for `tasks` and all of `building`'s calls, but not verified as a true ceiling for every possible call shape. If a future run throws `LLM_OUTPUT_TRUNCATED`, raise it further and re-verify by tail-checking output files, don't just suppress the error.
10. Add prompt/context caching (e.g. Anthropic's `cache_control`) for the grounding + contract docs in `llm-adapter.ts` — every one of the 32 Anthropic calls made 2026-08-01 resent the same ~150-200K tokens of grounding/contract-doc content uncached, which is very likely the dominant cost driver behind the $10+ burned testing this pipeline, independent of the truncation-bug waste.
11. Before implementing item 10: investigate whether Gemini (Vertex AI) and OpenAI also support context/prompt caching for the same supporting docs, and on what terms (API shape, minimum cacheable size, cache lifetime, cost model) — don't assume Anthropic's mechanism generalizes across all three providers in `llm-adapter.ts`.
12. `governance/reference-docs/firestore.rules.txt`/`firestore.indexes.json` are literal files in the cloned repo (`output/clones/<repo>/firestore/firestore.rules`, `.../firestore.indexes.json`) — P1 could copy them directly instead of relying on a hand snapshot, cheap/low-risk whenever picked up. `firestore-schema.md` is a machine-generated *live staging-DB introspection* (its own header: "Generated automatically on 2026-06-01..."), not sourced from the repo at all — capturing it ourselves would mean a **new P1 capability this pipeline has never had: live, credentialed Firestore access** (not just git clone + static AST). Same applies to RBAC (item 13). This is real new scope (new credentials, explicit staging-vs-prod project targeting in config — never defaulted/inferred, dev environment explicitly ruled out as unstable — and field-shape-only capture, never real document values). **Skipped for now** — deferred, P2 work takes priority.
13. RBAC roles are confirmed **live in Firestore** at `/settings/roles/compositeRoles` (and a sibling roles collection, per `role.service.ts`'s `OSKRoleController.default.save`) — the static `composite_role.data.ts` is the seed source, not the runtime source. A future live-capture script should read Firestore directly for the authoritative current state. Note: live schema introspection (item 12) can under-report vs. what P1's `model_property` facts already extract from TypeScript types, if staging data is sparse — treat live capture as a drift-detection supplement, not a replacement for existing type-level facts. Also see item 14: the RBAC file's own internal structure (composite vs. leaf roles) is a separate, already-resolved question from where the data is sourced from.
14. `rbac-roles.json` is a nested tree: composite roles (admin-facing bundles, e.g. "Manage Buildings") wrap child composite roles and leaf `roles` (the actual granular permission strings like `v1.org.buildings.create` that appear in code, each with a bilingual title/description). Investigated 2026-08-02: **the granular leaf roles are what's actually needed**, not the composite bundles — code checks the leaf string, and the leaf's own English description is exactly what caught the `v1.org.settings.create` semantic mismatch earlier. Todo: flatten the leaf roles into a deterministic `{permissionString: englishDescription}` lookup artifact (drop French, drop composite nesting) for P2's permission cross-check — first concrete Tier 2 artifact for the plan in item 15. Keep the full nested file (composites included) as the canonical source for any future impact-analysis use case that actually needs bundle structure — just don't pre-load it into every P2 call.
15. Finish the active plan: [`02-structural-narrative-synthesis-tiers.md`](02-structural-narrative-synthesis-tiers.md) (see `governance/adrs/adr-004.md`) — classifies P2 inputs into structural/deterministic-cross-reference/narrative tiers before designing any batching/recursive-reduce mechanism for the reduce step, so scaling to bigger modules or cross-repo synthesis doesn't silently lose dependency/ownership facts across bundle boundaries.
16. `contracts/00-capability-synthesis.md`'s "Citing evidence inline" section currently presents fact-ID and file-line citations as equally valid, with fact-ID only "preferred when the claim comes from one specific fact" — but they're not equally robust. File-line citations (`` `path.ts` (lines N-M) ``) are pinned to a specific commit's line numbers and go stale from ANY source change that shifts lines above that point in the same file — even a single comment added, no semantic change at all. Fact-ID citations don't have this problem: `stableFactId()` already excludes line number from identity (fixed earlier, Plan 03 Stage 1), so a fact-ID citation survives a comment addition untouched. Strengthen the contract to explicitly prefer fact-ID citations for this reason (not just as a vague preference), reserving file-line citations for cases with no single backing fact (e.g. a multi-fact code range). Separately worth noting as a mitigation already available without a contract change: re-running `validateCitations()` against freshly re-extracted Phase 1 facts (cheap, no LLM) against an *existing* document's text will correctly flag drifted file-line citations as "line-unverified" — a free staleness detector, not a reason a full re-synthesis is required after every source edit.

17. **LLM output variance in Phase 2 synthesis — confirmed real (measured 4x), root-caused down to specific contract sections via a multi-round review with an external model (ChatGPT), fully documented in `governance/roadmap/contract-refactoring/llm-output-variance-handoff-2026-08-30.md` and `governance/roadmap/contract-refactoring/contract-scope-conflict-review-*-2026-08-30*.md/_0N.md`.** Two independently-scoped fixes are ready for implementation, deliberately sequenced and tested separately (do not combine and evaluate only the final assembled profile — attribution of any improvement would be lost). Full scope: `governance/roadmap/v1-a-capability-synthesis-contract-scope-2026-08-30.md` and `governance/roadmap/v1-b-module-reduce-contract-scope-2026-08-30.md`. Atomic task breakdown:

    **V1-A (`00-capability-synthesis.md`) — capability-stage traversal fix:**
    - ~~17a.~~ **Resolved 2026-08-30, no longer a blocker.** Original concern: the Section 3 deterministic-assembly proof was based only on `apps/mail` (2 classes, tiny) — not representative of the codebase's real complexity. Re-checked against `supplierStaff` (605 facts, the largest single capability pack in the corpus, 10 classes) and two other large capabilities (`building_unit_nonAppUser`, `organization_intercom_communication`). Found two additional fact types near "public interface" territory in the complex cases that `apps/mail` never surfaces: `exported_symbol` (barrel-file `index.ts` re-exports — confirmed these just re-point at the *same* classes already captured via `source_class`, not a new category) and `function_declaration` (in `supplierStaff`'s case, `getCallableFunctionTriggers` — confirmed this is Cloud Functions registration wiring, not a business-facing interface, matching the same pattern already found in the RBAC investigation's `getRoleCallableFunction`/`getRoleFirestoreTriggers`). Neither represents a real gap. **`supplierStaff` should be the reference capability for 17b/17c/17e going forward, not `apps/mail`** — same conclusion, now backed by the pipeline's most complex real case instead of its simplest one.
    - ~~17b/17c.~~ **Done and verified 2026-08-30.** `buildPublicInterfacesSection()` added to `_shared/capability-synthesis.ts` (builds Section 3 from `source_class` + `controller_method`/`service_method` facts, exported classes and public methods only). `replaceNumberedSection()` added to `_shared/document-sections.ts` (heading-level-tolerant splice, fails loudly if the LLM omits the section entirely — mirrors `splitNumberedSections`'s own tolerance). Wired into both `01a-generate-capability-syntheses.ts` and `01d-regenerate-single-capability.ts`, patching the file after the LLM call returns and before the provenance sidecar is written. Contract text for Section 3 rewritten to tell the model not to write it (discarded and replaced regardless). `tsc --noEmit` clean; diff confirmed isolated to the 5 intended files.
    - ~~17d.~~ **Done 2026-08-30.** Section 2 rewritten: bounded candidate-source traversal list + the worked negative/positive grouping example from the scope doc, no fixed responsibility count.
    - ~~17e.~~ **Done and verified 2026-08-30, against two structurally different capabilities, not just one.** `supplierStaff` (10 classes, clean controller/service pairs) — Section 3 correctly enumerated all 10 classes with full method lists and real fact-ID citations. `organization_intercom_ communication` (3 classes, much higher call-expression density) — same correctness, all 3 classes enumerated correctly. One minor, non-breaking anomaly observed on the second run: the LLM nested its own `### 3.` header as a sub-heading inside Section 2's content instead of a peer top-level section — didn't affect correctness, since `replaceNumberedSection` is heading-level-tolerant by design, but worth watching if it recurs. Section 2's output read coherently in both runs. **V1-A's capability-stage half is complete; remaining V1-A work is 17f (informational, no code) plus whatever the shared A/B/AB experiment (17m-17o) still needs once V1-B also lands.**
    - **Correction found 2026-08-30, during the A/B/AB experiment analysis (see `06b-v1-ab-factorial-experiment-results.md`), fixed same day:** 17b/17c's "Section 3 is 100% deterministic" claim had one real exception — `organization_entity` (cap-run2) came back with Section 3 empty. Root cause: `splitNumberedSections`/`replaceNumberedSection`'s heading-level tolerance (1-6, added 2026-08-11 for a real, different reason — the LLM not reliably reproducing the exact `###` level a contract specifies) had no way to distinguish a document's real top-level sections from a coincidental digit-numbered sub-heading the LLM wrote *inside* another section's own prose (`#### 3. Entity Dashboard Statistics Aggregation`, nested inside Section 2) — the nested fake "3." was matched and spliced into instead of the real one, which was then left empty. **First fix attempt (prefer the document's shallowest heading level) was itself incomplete** — found immediately by re-testing against the real canonical `organization_intercom_ communication.md`, whose genuine Section 3 header happens to sit one level deeper (`###`) than all its other real sections (`##`), with no competing nested match anywhere: a pure shallowest-level rule wrongly discarded this real section too. **Final fix**: `selectRealSectionMatches()` — group candidate headings by number; if a number has only one candidate, keep it regardless of level (nothing to disambiguate against); if it has multiple candidates, prefer whichever sits at the document's canonical shallowest level. Verified against all four cases: the real `organization_entity.md` corruption (Section 3 correctly reported empty, nested fake heading stays inside Section 2), the real `organization_intercom_ communication.md` level-inconsistent-but-genuine case (all 10 sections now parse, Section 3 correctly non-empty), a synthetic repro of the nested-heading structure (`replaceNumberedSection` correctly targets the real header), and a known-good file (`organization_prompt_templates.md`, no regression). `tsc --noEmit` clean, diff isolated to `_shared/document-sections.ts`. Real end-to-end confirmation: a reduce call against `organization` that previously hard-failed on the corrupted canonical file now completes successfully.
    - **Second correction found 2026-08-30, same analysis:** the new reduce contract (already live in production) sometimes returns valid section headers but near-empty bodies for the genuinely judgment-heavy free-text sections (Permissions & Security's cross-cutting callouts, Architectural Observations, Risks & Open Questions) specifically on the largest sampled module (`organization`, 14 capabilities) — 2 of 4 sampled runs affected, never observed on `tasks`/`apps` under the same contract, never observed under the old contract on any module (confirmed by directly re-measuring section lengths across all 4 arms — Current/A never dropped below the healthy range on any of 4 organization samples each). Not a raw prompt-size effect: the same deterministic RBAC/unresolved-edge data reaches every arm's prompt regardless of which contract text is used. **Fix**: detect-and-retry in `01c-generate-assembly-first-profile.ts` — after the connective-tissue call returns, check the three judgment-heavy sections' body length against `MIN_JUDGMENT_SECTION_CHARS = 200`, a threshold set from real measured data (smallest genuinely healthy value seen across 18 real tasks/apps samples was 636 characters; confirmed-degenerate real samples were 37-116), and retry the whole call (up to 3 attempts total) if any fall short, logging `CONNECTIVE_CONTENT_DEGENERATE_RETRY`/`_FINAL` notifications either way. `tsc --noEmit` clean. Real verification: 4 fresh end-to-end `organization` reduce calls all came back healthy with no retry triggered — reassuring for the normal path, but since the original failure rate was ~50% on a 4-sample set, this doesn't yet prove the retry trigger itself fires correctly in production; the new notification codes will make the next real occurrence visible either way. Root cause of *why* the new contract text specifically (not data volume) triggers this more than the old one on large modules remains unexplained — this is a pragmatic detection/mitigation, not a fix to the underlying model behavior.
    - ~~17f.~~ **Done 2026-08-30 — negative result, hypothesis retired.** Re-ran the Section-8-vs-2/3 stability comparison across 9 modules beyond `apps` (using the existing on-disk Measurement 1 profiles, no new LLM calls; `supplier`/`organization` excluded since today's V1-A test runs patched two of their capability files). Result does not hold up: the "bounded" final-profile Section 11 (External Hooks, from capability Section 8) was *not* more stable than the "open-ended" Section 4 (Public Interfaces, from capability Section 3) — in most modules it was equal or worse (`building` -56%/-56%, `user` +57%/+175%, `settings` -12%/+100%, `admin` -35%/-67%). Most likely explanation: Section 11's totals are almost always tiny (1-19 citations), and small absolute counts produce large, noisy percentage swings regardless of the underlying instruction's boundedness — the `apps`-only result that originally motivated this comparison was very likely coincidental to that module's specific numbers. **This does not undermine Section 3's fix** (justified independently, on the grounds that Phase 1 already deterministically knows the answer — not on a "boundedness reduces variance" theory), but "bounded fact-type traversal reduces variance" itself should be retired as an unproven general hypothesis rather than carried into V1-B or applied elsewhere. Worth relaying back to the ChatGPT review thread — this directly revises a point logged there as "hypothesis, not finding."

    **V1-B (`01-module-synthesis-reduce.md`) — reduce-stage scope + wiring fix:**
    - ~~17g.~~ **Done 2026-08-30.** `01-module-synthesis-reduce.md` rewritten fully self-contained — the "read `module-engineering-profile-task-instructions.md` in full" dependency removed entirely. Inlined a "Shared Principles" section (evidence priority, confidence tagging, never invent, terminology preservation, RBAC cross-check, "don't treat the module in isolation") copied in directly rather than by reference. Added an explicit governing principle ("never repeat analysis assigned to an earlier synthesis stage") and a redefined, narrower Open Question scope for Section 13 (cross-cutting only — a capability-local question is not this step's job to re-raise).
    - ~~17h.~~ **Done 2026-08-30.** New `_shared/rbac-catalog.ts`: `filterRbacRequirementsForModule()` filters `04-build-resolved-graph.ts`'s repo-wide `rbacRequirements` catalog down to the checks whose `module` field matches the target module, grouped by permission with confidence tier, in-module check count, and which submodule(s) check it. Required one small additive fix in `04-build-resolved-graph.ts` itself: `checks.push(...)` wasn't recording `submodule`, only `module` — without it there'd be no way to attribute a check to a specific capability, which is the entire point of the per-capability tally. Wired into `01c-generate-assembly-first-profile.ts`'s reduce prompt as a new deterministic input section.
    - ~~17i.~~ **Done 2026-08-30.** Section 9's contract text no longer says "build a mental enforcement tally" — rewritten to say "you are given an explicit, pre-built RBAC Requirements Catalog for this module — reason from that table." The "is this asymmetry architecturally significant" judgment is retained as the one genuinely irreducible piece.
    - ~~17j.~~ **Done 2026-08-30.** Same pattern as 17h: `filterUnresolvedCallEdgesForModule()`/`formatUnresolvedCallEdges()` added to `_shared/call-edges.ts`, filtering `04-build-resolved-graph.ts`'s repo-wide `unresolvedCallEdges` to the target module (also required adding `sourceSubmodule` to the push, same gap as 17h). Wired into `01c`'s reduce prompt. Destructive-operations-without-permission-evidence and the other unaggregated candidates from the scope doc's table were correctly left out — no new extraction work performed.
    - ~~17k.~~ **Done 2026-08-30.** Section 6's contract text tightened to the scope doc's exact framing: the supplied capability extracts establish *which capabilities touch a path*, the ownership hints establish *call-graph centrality*, and neither establishes *architectural ownership* — the model's job is only the judgment that can't be established deterministically from them. Section unchanged as LLM judgment, not made deterministic.
    - ~~17l.~~ **Done 2026-08-30.** `tsc --noEmit -p .` clean. `git status` confirmed the diff isolated to the intended files (`04-build-resolved-graph.ts`, `01c-generate-assembly-first-profile.ts`, `_shared/call-edges.ts`, new `_shared/rbac-catalog.ts`, `contracts/01-module-synthesis-reduce.md`) plus the already-reviewed V1-A diff. Real test run: `organization` module reduce step run end-to-end in `COMPARISON_MODE` against `gemini-default` (isolated to `llm-comparison/gemini-default/organization/`, canonical baseline untouched). Output confirmed both new inputs are actually used, not just present in the prompt: Section 9 named a specific, real over-privilege finding (`v1.org.buildings.create` — "Allows to create a new building" per the RBAC roles doc — used to gate resident-invitation and onboarding workflows in `organization_building_invitation`/`organization_onboarding_inhabitant`), and Section 13 named two specific unresolved call edges by name and file:line (`processChannel` in `organization_intercom_communication.service.ts:1372-1373`; `generateAlphanumericCode`/`generateNumericCode` in `organization_onboarding_inhabitant.service.ts`). Citation validation: 0 file-not-found across both output documents. **V1-B is complete.**
    - **Correction found 2026-08-30, during A/B/AB experiment design (see `06-v1-ab-factorial-experiment-plan.md`):** `config/repos.json`'s `moduleSynthesisContractPaths` for `firebase-oskey-dev` still listed `module-engineering-profile-task-instructions.md` and `-template.md` alongside the reduce contract — the contract text's own rewrite (17g) never controlled what actually got fed into `01c`'s prompt, this config array did, and it was never updated. Fixed: trimmed to just `01-module-synthesis-reduce.md`. Re-verified against `organization` twice more post-fix: one run came back with an empty Section 13 and a truncated Section 9 lead-in, the next came back full and specific (same over-privilege and unresolved-edge findings as the original test, differently worded) — consistent with this thread's own baseline-variance finding, not a regression from the config fix. Angular/Node-IoT's copies of this same pattern were deliberately left untouched, per the standing rule that V1-A/V1-B stay Firebase-only until results are measured.

    **Shared experiment (after 17a-17l land):**
    - ~~17m.~~ **Done 2026-08-30.** Subset: `tasks` (1 pack), `apps` (4 packs), `organization` (14 packs) — see `06-v1-ab-factorial-experiment-plan.md` for the full design, including two real bugs found and fixed during design (organization's canonical capability output partly contaminated by a new-contract regen from 17e's own verification; `config/repos.json` still injecting the old instructions/template docs into every reduce call regardless of contract text).
    - ~~17n.~~ **Done 2026-08-30.** Provider/config confirmed (`gemini-default`, 10 new namespacing-only config keys added, same pattern as the existing `-temp0`/`-temp0-run2` pair). 63 real calls executed via a generated, shuffled driver script (`experiment-v1ab-factorial-runner.sh`) — order interleaved (fixed seed, verified no two self-consistency runs of the same cell adjacent) as insurance against any provider-side session/routing correlation, on top of the existing temp=0 evidence against literal caching effects. Completed successfully, exit code 0, no real errors (all "error"/"failure" string hits in the log are model-authored domain content, e.g. SMS/email failure-handling descriptions, not pipeline failures).
    - ~~17o.~~ **Done 2026-08-30.** Full measurement already existed in `06b-v1-ab-factorial-experiment-results.md` (analysis-only, no new LLM calls, written the same day as the experiment) — checked it against this task's exact required metrics (Section 3 semantic-inventory equality across cap-run1/cap-run2, Section 2 evidence-engagement overlap, Sections 6/9/13 per-arm run1/run2 self-consistency with fixed capability input, cross-arm comparison) and confirmed it satisfies all of them, not raw citation counts. **Bottom line, per section:** no arm shows a clear variance-reduction win for Section 6 (ownership conclusions were already near-zero-variance under Current on both `apps` and `organization`); **Arm A shows a real, substantial win for Section 9 on `organization`** (~100%/4-of-4 named-finding overlap vs. Current's ~33%/1-of-3), the single clearest result in the whole experiment; Section 13 shows no clear win for any arm (Arm A's ~67% overlap is not better than Current's ~83%). Both defects 06b flagged as blocking a fair read of B/AB are **confirmed already fixed in the current codebase**, independently of this task: the `organization_entity` cap-run2 Section 3 misassembly (fixed by `selectRealSectionMatches()` in `_shared/document-sections.ts`, same fix as 17e's own "Correction found" entry above) and the empty-cross-cutting-content failure on `b-run2`/`ab-run1` (fixed by the detect-and-retry loop in `01c-generate-assembly-first-profile.ts` — confirmed its `JUDGMENT_SECTIONS_TO_VERIFY` list checks exactly `PERMISSIONS_RISK` (Section 9, value 9) and `RISKS` (Section 13, value 13), the identical two sections 06b found completely empty on those two runs). **Re-run 2026-08-30 (same day, 4 real calls, no code changes):** re-ran exactly the `organization` `b-run1`/`b-run2`/`ab-run1`/`ab-run2` cells against the current codebase. **The empty-content failure did not recur in any of the 4 cells** — Section 9 and 13 fully populated in all four, 0 retries fired, `CITATION_VALIDATION_PASSED` on all outputs. With the failure gone, **B and AB both now show ~100% Section 9 named-finding overlap** (run1's findings are a strict subset of run2's), matching Arm A's original win almost exactly; Section 13 remains mixed for every arm including B/AB, unchanged from the original finding. Full detail: `06b-v1-ab-factorial-experiment-results.md`'s "Re-run: B/AB content-omission fix verification" section. **Revised porting decision (supersedes the V1-A-only call above): port both V1-A and V1-B to Angular/Node-IoT** — V1-B is no longer blocked by an unresolved bug, its Section 9 self-consistency is now demonstrated (not just theorized), and it has independent structural justification (17h/17j/17k) regardless. Caveat: this confirmation covered only `organization` and 4 calls, not a full re-run of the 24-cell reduce-stage experiment — a stronger guarantee would need that fuller re-run, not yet done. All 63 original + 4 re-run outputs remain on disk under `output/runs/firebase-oskey-dev/20260829_081559-00e1d9fd/llm-comparison/gemini-default-v1ab-*/`.

**Porting to Angular and Node-IoT: done and real-call verified, 2026-08-30.** Both ports ran through their own peer sessions, staged with checkpoints, mirroring this project's own review protocol. Neither was a copy-paste — both surfaced genuine repo-specific adaptations, confirming the "no repo-name conditionals, real content differences instead" requirement held throughout.
- **Angular**: `document-sections.ts`'s disambiguation/splice fix ported verbatim. Section 3's deterministic builder was genuinely rewritten (Angular has no controller/service-method concept — sources from `angular_component`/`angular_injectable` facts, grouped by kind instead). The real-call verification of this rewrite caught a substantive bug before it shipped: the builder filtered on `isExported`, a field that doesn't exist at all on those fact types (only `source_class` carries it) — would have silently emptied Section 3 on every real capability forever. Fixed by cross-referencing exported class names from `source_class` instead. V1-B's RBAC-catalog/call-edges wiring, retry loop, and `04-build-resolved-graph.ts`'s 2-line submodule fix all ported cleanly (Angular has no RBAC-roles document at all, so that one contract principle was reworded to degrade the same generic, config-driven way Angular's own `02-generate-repo-report.ts` already handles it — not special-cased prose). `config/repos.json`'s same stale-3-doc bug found and fixed. Both V1-A and V1-B real-call verified end-to-end on real capability/module data.
- **Node-IoT**: same mechanical pieces ported cleanly, plus two real structural findings. Section 3's builder needed a genuinely different two-tier rewrite (Route Handler tier from `route_definition.handlerClass`/`handlerMethod`, Controller tier from `controller_method` — no `service_method` role exists here at all), verified against every real fact in a test pack (4/5 controller methods correctly included, the 1 private one correctly excluded; all 3 route definitions matched exactly). `rbac-catalog.ts` was correctly **not** ported — confirmed this repo's reduce contract already made Permissions & Security fully deterministic with zero LLM-judgment slot to feed a catalog into, a pre-existing design decision independent of this port, not a "missing document" case like Angular's. Same stale-config-paths bug and submodule-recording gap found and fixed.

**Full-repo spot-check, 2026-08-30 (before porting):** ran 01a→01c (single run, live post-fix contracts, `COMPARISON_MODE=true` under a new `gemini-default-v1ab-fullrepo-validation` key so the verified canonical corpus was untouched) across 6 of the repo's 12 modules the original 3-module sample never covered — `access_control_device`(1 capability pack), `admin`(5), `apps`(4), `building`(11), `call`(1), `core`(6) — deliberately stopped there rather than running all 12: combined with `organization`(14, the repo's largest) and `tasks`(1) already validated separately, this spans the full pack-count range (1 to 14) the content-omission bug could plausibly scale with, so the remaining modules (`settings`, `supplier`, `unit_management` — all small, same tier as `access_control_device`/`call`; `user`, 11 packs, the one module with any real remaining marginal value) added little for the added ~15-20+ min of real LLM time. **Result: all 6 modules completed clean** — `CITATION_VALIDATION_PASSED` on every profile and API reference, zero `CONNECTIVE_CONTENT_DEGENERATE_*` notifications, zero `SHORT_ID_RESTORATION_INCOMPLETE` warnings. `building` (11 packs, the second-largest module in the repo) completing with no content-omission recurrence is the most load-bearing result here — it's an independent second large-module confirmation alongside `organization`'s own re-run, on a module that was never part of the original 4-cell fix verification. This is the real, full-repo-scale evidence base the V1-A/V1-B port to Angular/Node-IoT is now proceeding on.

19. **Architecture challenge: is per-capability fan-out still needed at all, given later fixes? Real feasibility test run 2026-08-30 — yes, this was a real rabbit hole.** Full findings: `09-fact-table-redundancy-reduction.md`. Triggered by a direct question during the caching work: the fan-out (one call per capability + one reduce call, 61+12=73 calls for a full Firebase repo) was built 2026-08-01 to fix a real context overflow (`building`, 1,376,733 tokens vs. a 1,048,576 limit) — but the SAME-DAY compact-table-encoding fix (~2.74x) was never re-checked against that number. Real per-module extrapolation showed even the repo's largest module (`organization`, 3,155 facts) should fit in one call (~549K estimated tokens). **Real test confirmed it**: one real call synthesizing all 14 of `organization`'s capabilities together succeeded at 659,011 input tokens (vs. ~1.21M across today's 15 separate calls for the same module) — no dropped capabilities, no quality collapse, all previously-validated findings recurred, and one genuinely new real finding surfaced (a global Firestore rules RBAC bypass) that no prior fragmented-view run this session had caught. Separately found and fixed real waste in the fact-table encoding itself (`factsToCompactTable` in `phase-01-ast-extraction/_shared/run-utils.ts`): `module`/`repo`/`runId` were 100% constant across every fact and repeated on every row anyway (now hoisted into a one-line preamble, live for every caller, 10.9% real reduction, zero risk); the `id` column was 35.2% of the table, duplicating data already on the same row (new opt-in `factsToCompactTableShortIds()`/`restoreFactIdCitations()`, not yet wired into production, real measured additional 38.6% reduction). **Combined, verified end-to-end**: 382,253 real input tokens for the whole `organization` module in one call — a 68% total reduction vs. the original 15-call architecture, with 210/210 real citations verified after fixing a real bug in the citation-restoration regex (didn't account for this pipeline's actual nested-backtick citation format) found during this same verification. Explicitly not confused with the separate, later fact-to-workflow indexing need (durable identity — the real fact `id` field already satisfies that; short IDs are round-trip-only and never persisted). **Not yet decided or wired into production** — still needs: batching strategy for a module too large for one call (deferred), self-consistency measurement, and a real decision on how this interacts with the caching work in item 18 (finding: caching's priority drops substantially under this architecture, since the module-specific facts — not the cacheable stable prefix — now dominate each call's cost).

**Real production cutover DONE, tracked in `10-module-level-production-cutover-plan.md` — Part A (all 7 steps) complete 2026-08-30.** New production script `01e-generate-module-level-profile.ts` + contract `contracts/03-module-level-synthesis.md` + `config/repos.json`'s new `moduleLevelProfile` block are the real, live path for Firebase now — `01a`/`01c`/`01d`/`capabilityBasedProfile` deliberately kept as the fallback for a module that ever exceeds the loud-failure size threshold (batching remains deferred, not solved).

Real, verified results across a full 12-module batch (`gemini-default`, COMPARISON_MODE): every module completed, every citation-validation check passed (0 file-not-found anywhere). **Real timing, not just token savings**: all 12 modules combined took 8m 28s wall-clock total — less time than the OLD architecture took for `organization` alone (19m 23s: 14 sequential capability calls + 1 reduce call, real notification timestamps). Token savings confirmed at scale too: 68% reduction for `organization` specifically, holding across the size range tested.

Bugs found and fixed during the real, at-scale verification (not just the earlier smaller tests): a narrowed citation vocabulary (file+line citations were silently dropped, restored), a malformed-citation silent-loss bug (range citations like `F201-F203`, worse at scale than first seen — 20 occurrences across the 2 largest modules — fixed with real per-fact expansion, `expandShortIdRangeCitations()`), a dangling-backtick artifact in footnoted file-line citations (real observed nesting had the outer close AFTER the parenthetical, not before), a Section 5 JSON-dump explosion (73% reduction via a proper formatter), single-capability module naming (model wrote the module's own name instead of the real `_module_root` submodule id — confirmed recurring on a second, different single-capability module, validating the deterministic remap fix generalizes), and — the biggest one — footnote-style citation rendering (`renderCitationsAsFootnotes`/`formatEvidenceAppendix`/`resolveFootnotesForValidation`), which shrinks the readable body ~60% while keeping every citation exactly as checkable, plus blank-line spacing between narrative bullets. One genuine false alarm chased and resolved during this work: `addNotification`'s upsert-by-ID behavior can leave a stale warning sitting in the log from an earlier failing run, indistinguishable at a glance from a live recurrence — resolved by checking real timestamps and grepping the actual current documents directly, not trusting the log's last-N-entries view alone.

**Angular/Node-IoT porting (Part B): done, 2026-08-31**, via peer sessions on each repo, staged with checkpoints throughout. Feasibility checked first (pure token-math, no LLM calls): node-iot's one module comfortably fit (~166K-200K est. tokens, 3.5-4.2x margin under the 700K threshold); Angular's `components`(21K) and `core`(90K) comfortably fit but `features` did not (2.4x over even before Firebase's encoding fixes, real measured **930,540 tokens** — 32% over — even after) — confirmed not an encoding difference (per-fact token cost matches Firebase's within 2%), genuinely a bigger module than anything Firebase validated, and possibly a Phase-1 module-boundary artifact (Angular's `features` folds every `src/app/features/` business area into one Phase-1 module, unlike Firebase's one-folder-one-module convention) rather than a Phase-2 problem — flagged, not acted on, a separate future call. Decision: reuse Firebase's already-proven hybrid (one-shot module-level call as default, automatic fallback to the fan-out for anything over threshold) rather than building the deferred batching design — `features` simply runs on fallback, no regression, just no speedup yet.

**Both ports were real new authorship** (not adaptation) for the module-level contract and script, and both surfaced real, substantive corrections caught before shipping rather than after — the whole reason this was staged with checkpoints rather than run straight through:
- **Angular**: the `isExported` bug (Section 3 would have silently emptied on every real capability, forever — caught by a real-call test before shipping); Section 8 (Outbound Coupling) split into deterministic import-based + LLM-judged template-composition halves after discovering no graph resolves whether a composed element is a real custom component vs. third-party UI library; Section 12 (External Hooks) reverted from a planned deterministic builder back to LLM-authored after confirming it would regress real coverage (79 real files using `@angular/fire`/`@ngx-translate` that the `external_hook` fact family never captures); a structural `validateCitations()` scope gap found on Angular's first real test (a 100%-accurate cross-module citation flagged as fabricated) that turned out to affect Firebase's own code too, fixed on both (tasks.md item 24).
- **Node-IoT**: the two-tier Section 3 builder (Route Handler + Controller, no `service_method` role at all here); `rbac-catalog.ts` correctly NOT ported (this repo's reduce contract has zero judgment slot for RBAC content, a pre-existing decision, not a missing-document case); an Internal Structure design correction caught by checking Firebase's actual code rather than the old contract's stale framing; and a citation-pattern allowlist gap that silently un-footnoted 43% of real citations on its very first test run, which prompted checking (and finding) a dormant version of the same gap in Firebase's own patterns (tasks.md item 23 — a systemic finding, not yet structurally addressed).

Real numbers, verified not estimated: Angular's `core` (10 capabilities) came back 155/155 citations clean on its real test; node-iot's `access_control_device` came back 123/123 clean after its citation-pattern fix. All three repos' phase-2 orchestrators now share the identical hybrid pattern (module-level default, automatic fallback to the fan-out on the size-threshold error, matched on notification code+module+timestamp, not exit code). Nothing committed anywhere — that's still a deliberate, separate decision.

18. **Gemini context caching — real, measured cost-reduction opportunity. Implemented and verified 2026-08-30.** Scope + verification result: `07-gemini-context-caching-plan.md`. Triggered by a real billing review (34.28M input tokens / €45.14, 3.72M output tokens / €29.35) during the V1-A/V1-B experiment. Measured directly against real prompts: ~68% of tracked input token volume (blended across 305 capability calls + 80 reduce calls) is an identical, repeated stable prefix (contracts + all 6 grounding docs) that Gemini's `callGemini` path was resending in full on every single call — `CACHE_BREAKPOINT_MARKER` already existed and was real for Anthropic, but was a confirmed no-op for Gemini. Real Gemini 3.5 Flash pricing (checked directly): $1.50/M standard input vs $0.15/M cached-content input (90% off cache hits). Fix: added the missing stable/variable split to `01c`'s reduce prompt (capability calls already had it), new `_shared/gemini-cache.ts` (content-hash-keyed registry, real `ai.caches` wrapper, degrades to uncached on any failure), wired into `callGemini`. `tsc --noEmit` clean. **Real two-call verification**, each call a separate OS process against the real Vertex AI API: call 1 created a real cache resource and immediately read 43,632 of its 54,334 input tokens from it (matching the ~44K predicted stable-token estimate); call 2, a fully independent process, reused the *same* cache resource (confirmed via unchanged `cacheName`/`createdAt` in the registry) and reported the identical 43,632-token cache read. Not yet run at full-module or full-repo batch scale — next step, along with a real before/after billing comparison. Also surfaced, logged separately (not part of this task): ~66% of billed *output* tokens are untracked by the pipeline's own notification log, because the retry wrapper only logs usage for the attempt that finally succeeds — a visibility gap, not a cost-reduction lever, worth its own fix later.

20. **Citation-pattern fact-type allowlist was missing `function_declaration` — real, live gap in production citation validation, found during the Angular module-level port and confirmed independently on Firebase, fixed and verified 2026-08-30.** Found on the Angular side first: `citation-validator.ts`'s `FACT_ID_PATTERN` and `run-utils.ts`'s `FACT_ID_CITATION_BLOCK_PATTERN` both hardcode a fact-type allowlist that never included Angular's own fact types (`angular_component`, `angular_injectable`, `angular_route`, `angular_guard`, `angular_signal`, `angular_template_composition`, `angular_template_binding`, `firebase_callable_call`) — confirmed live, not theoretical: a real assembled document (`core-engineering-profile.md`, produced during the V1-A/V1-B port's stage-4 test) had 17 real citations of these types, all silently invisible to `extractCitations()` — the document's own "205 citations checked" notification was accurate for what it checked, just checking a smaller set than the document actually contained. Fixed on Angular's side (both patterns), verified against the same real document (205→222 citations, +17 exactly, 0 missing) and a footnote-rendering round-trip. **Separately, the Angular investigation surfaced that `function_declaration` was ALSO missing from both of Firebase's own original patterns — not Angular-specific, inherited by the port, not introduced by it.** Checked directly against real Firebase production output: 3 real `function_declaration` citations in `knowledge-corpus/firebase-oskey-dev/20260827_163338-1aa319b1/engineering-profiles/organization-engineering-profile.md` alone (from `getCallableFunctionTriggers`/`getCallableResidentsFunctionTriggers`), confirmed present across at least 5 other already-shipped documents (`tasks`, `admin`, `building`, `supplier` engineering profiles too, via a repo-wide grep) — meaning citation-validation's "0 file-not-found" claims throughout this entire session were accurate for the citations they checked, but were checking a citation set smaller than several real documents actually contain. Fixed both patterns in Firebase's own `citation-validator.ts`/`run-utils.ts` to include `function_declaration`. `tsc --noEmit` clean, diff isolated to the 2 intended files. Verified against real data: `extractCitations()` on the same `organization-engineering-profile.md` went from (implicitly) 232 to 235 citations, all 3 `function_declaration` citations now correctly extracted. Not yet re-run against every historical document to get an exhaustive count of previously-missed citations repo-wide — the fix is live for any future run; a full retroactive audit of already-shipped documents hasn't been done and isn't blocking anything, since none of those documents are being regenerated as a result of this fix alone.

21. **Minor, unconfirmed efficiency question, flagged during the Angular module-level port, not acted on: `03-module-level-synthesis.md` asks every capability for a "Summary" paragraph that `01e`'s assembly step never uses.** Confirmed directly: the contract's per-capability instructions ask for `1. **Summary** — one paragraph...` under a `### Summary` header, but `01e-generate-module-level-profile.ts`'s `assembleAcross()` calls never reference `"Summary"` anywhere in the final document assembly — every module's every capability generates this paragraph and it's silently discarded every time. Likely deliberate reasoning scaffolding (helps the model organize its thinking before writing the sections that DO get used) rather than a bug, and removing it isn't obviously safe without testing whether it's load-bearing for the *quality* of the sections that follow it — not fixed, just recorded so it isn't silently carried forward unexamined. If picked up: cheap to test (regenerate a couple of real capabilities with the Summary instruction removed, diff the sections that ARE used for any quality regression) before committing either way.

22. **Real Phase 1 extraction gap found during Angular's module-level port, 2026-08-30: the `external_hook`-family fact detection can't see the specific SDK usage its own capability contract promises to cover, on Angular at least.** `01-extract-ast-evidence.ts`'s 4 real trigger patterns (`.topic(...)` calls, publish chains, known wrapper methods, `process.env` references) are confirmed real and correctly firing zero matches on Angular's real cloned source — Angular genuinely has no Pub/Sub SDK usage and uses `environment.ts` config, not `process.env` (a real, expected frontend/backend difference, not a bug). But `00-capability-synthesis.md`'s own Section 9 text promises coverage of "Firebase SDK usage, `@ngx-translate`, or other injected external SDKs" — and **79 real files** in Angular's source import `@angular/fire`/`@ngx-translate` (confirmed directly, e.g. `current-user.type.ts` imports `User` from `@angular/fire/auth`), none of which flow into any `external_hook`-family fact type. This SDK-import evidence only exists as generic `imports_dependency` facts. **Consequence caught before shipping**: a deterministic Section 12/External Hooks builder built to read only the `external_hook` family would render permanently empty for every Angular module, forever — a real regression versus today's free-form LLM-authored version, which can and likely does notice these SDK imports in its prose since it isn't restricted to one fact type. **Decision: Section 12 stays LLM-authored in Angular's new module-level contract** (reverted from an initially-planned deterministic builder), matching the "err toward more judgment when determinism isn't proven better" default — except here it's not just theoretical risk, it's a measured regression. **Not fixed, deliberately out of scope for the port**: the real fix would be extending Phase 1's external-hook detection to recognize a real SDK-import allowlist (e.g. `@angular/fire`, `@ngx-translate`, and Firebase's own equivalent SDKs) as its own fact type, distinct from generic `imports_dependency` — worth its own task, and worth checking whether Firebase's own external-hook detection has an analogous blind spot for its own SDK-import patterns, not yet checked.

23. **Systemic pattern found, 2026-08-30, across the citation-pattern allowlist fix (item 20) and node-iot's own port: hardcoded fact-type-name allowlists silently drift from each repo's real fact vocabulary, and this has now caused real, measured harm on 2 of 3 repos, not just a theoretical risk.** Node-iot's own port hit this for real on its first test run: `FACT_ID_CITATION_BLOCK_PATTERN`'s ported (Firebase-derived) whitelist had no entry for node-iot's own real fact types (`route_definition`/`mongo_operation`/`joi_schema_field`/`pubsub_operation_route`), so 63 of 146 real citations (43%) silently stayed as full un-footnoted inline citations instead of being compacted — not a correctness failure (every citation was still valid), but a real, measured failure of the readability mechanism the pattern exists for, caught only by reading the actual assembled output, not by any clean exit or citation-validator pass (both look identical either way, since un-footnoted-but-valid citations don't fail validation). Fixed for node-iot by replacing the whitelist with its own real 17-type vocabulary, re-verified (0 un-footnoted node-iot-type citations, 123/123 verified). **Checked Firebase's own vocabulary against its own pattern per node-iot's suggestion**: found one real gap — `environment_variable` is a real, live fact type (confirmed emitted in `01-extract-ast-evidence.ts`/`02-build-module-evidence.ts`/`04-build-resolved-graph.ts`) that was never added to either `FACT_ID_PATTERN` or `FACT_ID_CITATION_BLOCK_PATTERN` — but confirmed **zero real occurrences** of this fact type across every run's extracted facts to date, so this is a live latent gap, not a currently-active bug for Firebase (nothing to silently mishandle yet). **Not fixed as a structural matter**: three real instances of the same failure mode in one session (Angular's 8 fact types + `function_declaration`, node-iot's 4 fact types, Firebase's own dormant `environment_variable` gap) suggests the hand-maintained-hardcoded-list design itself is fragile, not just under-maintained — worth considering a single source of truth (e.g., deriving the allowlist from each repo's own real fact-type list rather than a hand-copied regex) or a more general matching shape that doesn't require enumerating every type name at all. This is a design decision, not something to unilaterally implement — flagging for a decision, not yet actioned.

24. **`validateCitations()` had a structural scope gap under the module-level architecture — a real, accurate citation could be misflagged as fabricated purely because it pointed at another module's file. Found during Angular's real test run 2026-08-30, confirmed present in Firebase's own code too, fixed and verified same day.** Angular's real test cited `features/home/home.component.ts` (line 17) as evidence for a real circular-dependency finding in Architectural Observations — checked directly against the real source file, 100% accurate, sourced legitimately from the real `cross-module-dependencies.json` fed into the prompt. But `validateCitations()` only checks file-line citations against the CURRENT module's own `facts` array, so any citation pointing at a file outside that module always read as `CITATION_FILE_NOT_FOUND`, regardless of accuracy. **Confirmed this is not Angular-specific**: checked Firebase's own real `cross-module-dependencies.json` (module `organization`) directly — its `inbound[].touchpoints[].file` entries are genuinely other modules' files (e.g. `access_control_device`'s own file, listed under `organization`'s inbound dependencies) by design, so the identical exposure exists in Firebase's `01e` today. It hasn't produced a real false alarm yet (checked: zero `CITATION_FILE_NOT_FOUND` notifications anywhere in Firebase's real run-notifications.json to date) — likely because Firebase's cross-module data feeds a deterministic section (Cross-Module Relationships) rather than free LLM prose as often as Angular's Architectural Observations does, not because the gap doesn't exist. **Fix**: `validateCitations()` takes an optional `extraFileLines` parameter (additional known file:line pairs, checked the same way as the module's own facts, only affects the file-line branch — fact-ID matching is unaffected, since a module's LLM call never has another module's fact IDs to cite in the first place). Threaded through `writeProvenanceSidecar()` (which calls `validateCitations()` independently for the sidecar's own count — both call sites needed the fix to stay consistent) and wired into `01e-generate-module-level-profile.ts`, which flattens `cross-module-dependencies.json`'s real outbound+inbound touchpoints once and passes them to every validation call for that module. `tsc --noEmit` clean, diff isolated to the 3 intended files. **Verified against real data**: a real inbound touchpoint file:line pair from `organization`'s own `cross-module-dependencies.json`, cited in a synthetic document text scoped to a different module's facts — before the fix, 1 fileNotFound/0 verified; after, 0 fileNotFound/1 verified. Not yet ported to Angular/node-iot's own copies of `citation-validator.ts`/`provenance-sidecar.ts` — same fix needed there, since both now run the same module-level architecture with the same exposure.

25. **Module-level architecture variance check, 2026-08-31: the fixes validated on the old fan-out architecture (17o) were never tested on the architecture that's now primary across all 3 repos. Real test run — mixed result, plus two real pipeline bugs found and fixed along the way.** Design: Test A (2 real calls, temp=0.2, production setting) + Test B (2 real calls, temp=0.0) against each repo's largest/most complex real module — `organization` (Firebase, 14 capabilities), `core` (Angular, 10 capabilities), `access_control_device` (node-iot, its only module) — 12 real calls total.

    **Two real pipeline bugs found during Angular's run, confirmed present in Firebase's own `01e` too, both fixed same day:**
    - **`MODULE_LEVEL_SECTION_MISSING`'s content check only verified a section header existed, never that its body was non-empty.** Angular's `varcheck-a-run1` produced a completely empty Cross-Cutting Risks section (header present, zero content) and this fired zero warnings — exactly the failure class 01c's `MIN_JUDGMENT_SECTION_CHARS` retry loop was built to catch on the old architecture, but that safeguard was never ported to either repo's `01e`. **Fixed** (Firebase's copy): `missingModuleWide` now also flags any section whose body is under 200 chars (same threshold 01c uses), not just an absent key. Verified with a synthetic before/after (empty string correctly flagged, a real 250-char string correctly not). **Deliberately not doing the retry half yet** — a full one-shot regeneration costs far more to retry than 01c's reduce-only retry did, so this is detect-and-warn only until a real cost/benefit call is made on whether to auto-retry.
    - **Several of `01e`'s own notification IDs didn't include `LLM_CONFIG_KEY`, so multiple comparison runs against the same module could silently overwrite each other's log entries.** `addNotification`'s upsert-by-ID behavior (already known to cause stale-entry confusion earlier this session) turned this into a real misattribution: Angular's team initially blamed the wrong run for a missing-capability warning based on the notification log alone, and only caught the real culprit by reading the actual output file directly. Root cause: `MODULE_LEVEL_SECTION_MISSING`, `SHORT_ID_RESTORATION_INCOMPLETE`, `SINGLE_CAPABILITY_NAME_REMAPPED`, and `MODULE_TOO_LARGE_FOR_SINGLE_CALL` all used a bare filename or no file at all in their `details.file`, unlike `CITATION_FILE_NOT_FOUND`/`CITATION_VALIDATION_PASSED`/`MODULE_LEVEL_PROFILE_COMPLETED`, which already correctly prefix with the `LLM_CONFIG_KEY`-bearing `outputLabel`. **Fixed** (Firebase's copy): all 4 now include the same `outputLabel`-prefixed (or `LLM_CONFIG_KEY`-suffixed, for the one that fires before `outputLabel` exists) path. `tsc --noEmit` clean, diff isolated to `01e-generate-module-level-profile.ts`. **Not yet ported to Angular/node-iot's own copies** — same fix needed there.

    **The variance/quality findings themselves, real and replicated across all three repos:**
    - **Temp=0 is not tighter than temp=0.2 on citation-count variance, and was actually looser in 2 of 3 repos**: Firebase 14.4%(0.2) vs 16.4%(0.0); Angular 4.0%(0.2) vs 9.8%(0.0); node-iot 6.7%(0.2) vs 8.4%(0.0). This directly contradicts the intuitive "temp=0 reduces variance" assumption raised earlier in this same session.
    - **Node-iot's real content comparison is the clearest single result**: one temp=0.2 run uniquely caught a real, verified bug (a Pub/Sub delete handler dispatching to `res.status(200).send` with no actual database call — confirmed against the real `pubsub_operation_route` fact) that both temp=0 runs, despite closely agreeing with *each other*, both missed. Temp=0 bought self-consistency without buying completeness — it reliably reproduces whichever subset of real findings its greedy path happens to land on, not a superset or a "safer" answer.
    - **Firebase's `organization` shows a different pattern**: even the two temp=0 runs didn't converge with each other on the second-most-prominent risk finding (Privilege Escalation vs. Cascading Deletion Complexity) — real infra-level non-determinism appears to get more room to manifest on a larger, more complex module/longer generation, so node-iot's clean temp=0-self-consistency finding doesn't generalize to every module size.
    - **The one thing that stayed rock-solid everywhere, on every arm, every temperature, every repo**: the single most obvious/well-evidenced architectural finding for each module (Firebase: `Unresolved Call Edges`, deterministically sourced; Angular: the `core`→`features` circular-dependency/layering violation, LLM-authored but overwhelmingly evidenced) recurred in all 4 runs every time. Secondary, less-obvious findings are what actually vary.
    - **Bottom line**: no basis found for switching to temp=0 as a production standard — it doesn't reliably reduce variance, and the one repo where it looked more self-consistent (node-iot) achieved that by narrowing coverage, not by being more correct. Current temp=0.2 stays the standard. Sample size is 2+2 calls per repo (12 total) — suggestive, not conclusive, same caveat the original 17o work carried.

26. **A third instance of the same "higher temperature → small formatting liberties break strict parsers" pattern, found via node-iot's temp=0.4 run, confirmed present in Firebase's code too, fixed same day.** Node-iot's run1 (temp=0.4) had the model write `` ## CAPABILITY: `_module_root` `` (backtick-wrapped) instead of the plain form both contracts specify — the parser's exact-string match against real pack names never matched the wrapped name, so `MODULE_LEVEL_SECTION_MISSING` fired for all 6 capabilities even though their content was present and correct. run2 (same module, same facts, temp=0.4) used the plain form — confirmed temperature-dependent and non-deterministic, not a contract-text problem, and (so far) specific to temp=0.4 — never observed at 0.0/0.2 across any run this session, same as the short-ID-range malformation in item 25's temp=0.4 addendum. **Checked Firebase's own `01e` directly rather than assuming it was node-iot-specific**: `parseModuleLevelResponse()`'s capability-name extraction (`header.slice("CAPABILITY:".length).trim()`) never stripped backticks either — same exact-match vulnerability, confirmed by direct code read. Didn't happen to trigger in either of Firebase's own 2 temp=0.4 runs against `organization`, but the code gap is real regardless of whether a given sample happened to hit it — same principle node-iot itself applied when porting the citation-range fix without confirmation it would have bitten it. **Fixed**: strips an optional single-backtick wrapper from the parsed capability name before matching, via `.replace(/^\`(.+)\`$/, "$1")`. `tsc --noEmit` clean. Verified with a synthetic test: both the backtick-wrapped and plain forms now normalize to the identical name. Node-iot verified its own fix against the real captured response that triggered it (re-parsed with the fix applied, no new LLM call needed — all 6 capabilities correctly matched). Not yet confirmed whether Angular's own capability-name parsing has the same gap — worth the same proactive check, especially since its own 2 temp=0.4 runs also didn't happen to trigger it (same "absence of evidence isn't evidence of absence" caveat).

    **Content findings across the full 3-temperature, 2-repo (so far) dataset, updating item 25:** on Firebase's `organization`, the pattern was mixed (temp=0.2 tightest on citation-count, temp=0.4 tightest on content-agreement). On Angular's `core`, **temp=0.2 (the current production default) was the worst-performing condition on two separate axes**: both its runs had a real structural completeness defect (an empty module-wide section on one run, an entire missing capability on the other — see item 25's own two-bugs section, which is what surfaced this), while all 4 temp=0.0/0.4 runs were structurally clean; and its two runs each picked a different secondary risk framing, while 4 of the other 4 runs (both 0.0, both 0.4) converged on the same one. Node-iot's own temp=0.4 pair showed no correlation between temperature and *which* finding surfaces (5/6 runs across all 3 temperatures agree on finding #2, the one outlier is a temp=0.2 run) — consistent with Angular's direction, not Firebase's. **Still n=2 per condition per repo — not acted on, but the cross-repo direction (temp=0.2 underperforming on completeness/agreement, not just citation-count) is now visible in 2 of 3 repos and worth a real, larger follow-up before any production change, not a decision from this sample.**
