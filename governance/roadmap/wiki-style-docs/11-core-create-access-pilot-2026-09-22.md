# Core createAccess pilot completion

Coverage was reconciled before page generation: 44 operation facts (one
declaration, 43 call facts) and 28 direct operation-edge IDs. OSKAccessService
has a unique source-class declaration at `access.service.ts:68`. Its prior
125-fact/68-edge service totals remain the declared service scope; 664 of
1,798 core facts lack class/caller ownership fields and are not assigned.

The earlier export is selected-field evidence. The complete non-embedding
operation payload and direct-edge export is
`core-hub-retrieval-coverage/create-access-complete-semantic-export.txt`.
All 44 operation facts and 28 edges were exported; the page uses compact
meaningful summaries, while long payloads remain local.

Page: `pilot/core-create-access.html`. Static checks: unique internal anchors,
no scripts, CDN, network requests, or framework dependencies. Browser visual
rendering was not tested. The page is manually assembled, not a repeatable
generator.

The operation supports a next generator-design step: package declarations,
complete owned call facts, direct edges, and explicit gaps; require count
reconciliation before model synthesis. It does not establish transaction,
condition, authorization, error, helper, or business-intent behavior.
