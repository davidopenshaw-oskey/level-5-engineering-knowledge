# Building-operation HTML pilot completion

Output: `pilot/building-create-non-app-user-with-access.html`.

Corrections: non-app access is now an onboarding/access mechanism rather than
a persona; the authority principle is described as documented; direct
citations support the permanent helper argument and one-year permanent-guest
limit; and shared-document links omit unreliable heading fragments while
retaining headings, line ranges, and excerpts.

Checks: targeted helper fact retrieved read-only; explicit internal evidence
IDs; inline CSS only; no scripts, network URLs, CDN/fonts, or build tooling;
and document links are correct relative to `pilot/`. Visual rendering was not
independently browser-tested.

This is a manually assembled presentation pilot, not a repeatable automated
generator. Next decision: whether to specify an automated generator and
citation-data model from this pattern.
