# Session handoff — 2026-09-10

Real, specific hand-off at session close, per this project's own discipline (precedent: `33-session-handoff-2026-09-09.md`). This session ran long and covered three real phases — quota investigation/fix, a real architecture challenge/discussion, and a two-pair precision pilot with a real, concrete finding at the end — all in one continuous session, not split across boundaries. Flagging that directly since it's a deviation from the usual one-mode-per-session discipline; the next session should treat this as a real, deliberate exception, not a new norm.

## Read first, in this order

1. **`governance/roadmap/mcp-direction/44-session-findings-not-yet-recorded-2026-09-10.md`** — five real discussion threads from this session that shaped later work but were never written down until session close. Read this *before* the numbered docs below — it recalibrates how to read them (especially: cost/token deltas are a HITL trip-wire, not a benchmark metric; the system's real job is pointing a developer at real evidence, not producing an autonomously-correct design).
2. **`governance/roadmap/mcp-direction/43-precision-pilot-consolidated-findings-2026-09-10.md`** — the real synthesis of this session's two precision-comparison pairs (vague vs. precise prompts, same underlying real requests). The single most important finding: nothing currently checks whether a claim's cited evidence actually *supports* the claim — only that the cited fact_id is real. A proposed fix (embedding-similarity between claim text and evidence descriptions) was examined with real examples and **explicitly decided against** (see `44-...md` §5) — the underlying gap is still real and open, just not to be solved that way.
3. **`governance/roadmap/mcp-direction/40-technical-proposal-only-low-freedom-experiment-2026-09-10.md`** — the real result that started this session's whole narrow-scope thread: a deliberately narrow, hard-budgeted skill produced the intercom case's Technical Proposal in 6 real tool calls (vs. 26+ under full scope), including a correctly-earned `[NEEDS CLARIFICATION]` marker. n=1 — never repeated.
4. **`governance/roadmap/mcp-direction/38-real-request-rate-data-across-runs-2026-09-10.md`** — real quota investigation (confirmed: `gemini-3.5-flash`/`gemini-3.7-flash` both sit on the generic 5/min default with no override, while 35 other real models get 15-400/min) and the retry-on-429 fix, now **confirmed working end-to-end twice** (real live 429s, both recovered by one retry).
5. **`governance/roadmap/mcp-direction/37-real-run-crash-tool-error-not-surfaced-2026-09-10.md`** — a real, permanent fix to `atomic-prd-agent.ts`: a fabricated tool argument no longer crashes the whole run; it's now returned to the model as a recoverable error.

## Real, structural changes this session — don't assume old state

- **`mcp-server/skills/prd/skill.v2.md` / `template.v2.md`** — a real, drafted v2 redesign, tested across five real live runs today (1a, three attempts at 2a, and the "1"/"2" precision-pilot runs). **Not yet decided whether it replaces the live pair** (`mcp-server/skills/prd/skill.md`/`template.md`). Fixed today: both files originally leaked human-facing meta-commentary straight into the real system prompt (caught by the user) — now clean, matching the live pair's shape.
- **`mcp-server/skills/experiments/technical-proposal-only-low-freedom/{skill.md,template.md}`** — new, real, narrow experimental skill (one section only, hard tool-call budget). One successful real run (`40-...md`). The natural next step — a second run on a *different* section, to see if the clean result repeats — was proposed and never done.
- **`atomic-prd-agent.ts`** — two real, permanent, verified code changes: `withToolErrorTrapping` (a `[Fail-Closed]` tool error is now returned to the model instead of crashing the run) and `retryOn429` (a Genkit model-middleware that retries a single failing model call on 429, using Google's real `Retry-After` value when given, exponential backoff otherwise). Both confirmed working against real live failures, not just unit-tested.
- **`mcp-server/gold/evals/*.eval.json`** — 3 new real eval checklists (Anthropic's own `{query, expected_behavior}` shape) for the 3 existing gold cases, used to reason about the v2 sketch.
- **`mcp-server/gold/debug-logs/*.jsonl`** — real, preserved `DEBUG_TOOL_LOG` traces from this session's key runs (the cancelled intercom v2 run, the intercom Technical-Proposal-only experiment, and both precision-pilot pairs) — real data for future analysis, not sampled.
- **`governance/roadmap/market-research/22-findings-vertex-ai-generatecontent-rpm-quota-2026-09-10.md`** — real, external research confirming this project's quota problem is widely reported; the practical fix that worked elsewhere (retry-with-backoff) is what got built and confirmed today. An earlier draft priming-brief for this research leaked real project-identifying config (project ID, internal paths) into what would have been an internet-facing search prompt — caught before use, deleted, replaced with a clean, generic prompt. Worth remembering as a real, general caution for any future external-research handoff, not specific to this one incident.
- **Local Postgres** (`facts-postgres-index-local` Docker container) was stopped at session start, restarted for this session's live tests, and left running. Check its status before any new live run.

## Real, still-open decisions — not resolved this session

- **The claim-evidence-relationship gap is real and still open** (per `44-...md` §5) — individually-real facts can still be stitched into an unverified relational claim (order, completeness, causality). One proposed fix was explicitly ruled out; no alternative has been proposed. This is arguably the single most important unresolved finding from today.
- **Whether to request an actual Vertex AI quota increase** for `gemini-3.5-flash`/`gemini-3.7-flash` — still not done. The retry-on-429 fix makes the 5/min ceiling survivable for most runs, but doesn't remove it; a sufficiently long or chatty run could still exhaust the fix's 5 retry attempts.
- **Whether v2 (`skill.v2.md`/`template.v2.md`) replaces the live pair** — real, positive evidence accumulated today (zero regressions found across five real runs, one correctly-earned honest-gap marker, the retry fix now proven), but no formal decision made.
- **The "series of skills" / multi-page architecture idea is deliberately parked**, not decided either way (`44-...md` §1) — needs a second real narrow-scope data point before it's worth revisiting.
- **Whether to run a third precision pair, or pivot to something else** — this is exactly where the session ended before the close request came in; genuinely undecided.
- **The intercom business request still isn't in `test-questions.md`**, despite being this project's single most bug-surfacing real case this week (per `adr-009.md`). A cheap, obvious addition, not yet made.

## What this session should not re-litigate

Don't re-propose the embedding-similarity claim-evidence check — it was examined with real examples and explicitly decided against, for a specific, real reason (`44-...md` §5), not just deprioritized. A *different* approach to the same underlying real gap would be worth hearing, but that exact proposal has already been tested against a real counterexample and found wanting.
