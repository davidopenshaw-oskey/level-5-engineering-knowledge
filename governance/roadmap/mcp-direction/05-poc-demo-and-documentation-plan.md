# Path to POC Demo — Work Breakdown

**Status:** Written 2026-09-06, reflects real state as of that date — the MCP Layer session's Steps 1-2 (tool server) and most of Step 5 (first persona + PoC agent) are further along than this doc's own tasklist predicted when written. Update this doc's checkboxes as things actually close, don't let it drift stale the way a plan can when the real work outpaces it.

## 1. MCP tool server

- [x] Steps 1-2 built and verified (`04-step1-2-mcp-server-built-and-verified-2026-09-06.md`): three tools, real MCP-protocol test via a spawned client, known-case reproduction confirmed.
- [x] `walkBoundedCluster()` real prerequisite closed, including a genuine new finding (hub-dominance truncation) fed forward into the persona (`03-...md`).
- [ ] Interactive real-client check (Claude Code/Claude Desktop pointed at the local server) — Step 2's second method, flagged open in `04-...md`, not silently skipped.
- **Deliberately benched for POC:** Steps 3-4 (Cloud Run, Gemini Enterprise registration) — real, explicit decision, not a gap. A local MCP server is sufficient to prove the architecture for a demo; revisit only if the demo specifically needs to show it running inside Gemini Enterprise.

## 2. Agent personas

- [x] `atomic-prd-agent` persona written and a real PoC runner built (`agent-poc/atomic-prd-agent.ts`), using Genkit's real agentic loop (`ai.generate()` with tools, `maxTurns`), not a scripted pipeline.
- [x] **The real, decisive test — done, 2026-09-06, real positive result:** ran against the real Q1a/Q1b business requests. One persona, no query-specific tuning, closed both gaps the hardcoded pipeline never closed together — Q1b in particular now surfaces the real Angular UI components and all three real assignment options that the hardcoded pipeline's own recorded output (`output/atomic-prds/assign-building-unit-to-ownernonresident.md`) never found. Two real infra issues hit and fixed along the way (an under-budgeted `maxTurns`, a transient embedding-endpoint 429 — fixed with a real, quota-data-driven retry-with-backoff in `_shared/embedding-adapter.ts`, not a guessed sleep). Full write-up, real numbers, and honest caveats (small sample size, `maxTurns` headroom, real token cost): `06-step6-real-verification-q1a-q1b-2026-09-06.md`.
- [x] **Done, 2026-09-06 — token usage + real approx. cost, both live in `MetaData`.** The one real unverified question — whether the Catalog API's SKU granularity maps to Gemini's token categories — is answered: yes, more finely than expected (separate SKUs per modality/direction/caching state), except "thinking" tokens have no distinct SKU and bill at the plain output rate (checked directly, not assumed). Real, live pricing queried from the Cloud Billing Catalog API (`services/C7E2-9256-1C43`, the real Vertex AI service), matched to exactly one confident SKU per input/output/cache kind — fails closed to "unavailable" rather than guessing if the match isn't confident. Every cost figure carries the caveat requested directly by the user: it's an approximation of this one document's own Vertex AI token cost, explicitly not a real total cost of ownership (excludes subscriptions, infra, other overhead) — baked into the rendered line itself, not a separate footnote. New file: `mcp-server/agent-poc/pricing.ts`. Full detail: `10-sectioncontent-implementation-tasklist.md`'s eighth addition.
- [ ] Second persona: `impact-analysis-agent` — real, differentiated test case (ADR-006 already reasoned this is graph-traversal-answerable, not narrative-synthesis-heavy — a good proof that the tool layer generalizes across genuinely different question shapes). **Real, current market validation found 2026-09-06**: Allstacks Product Studio's "adversarial review findings" — scores a proposed spec against feasibility/capacity/security/historical-rework-rate before work is greenlit, its own materials call it a pre-mortem (`market-research/06-findings-product-team-artifacts-2026-09-06.md`). Functionally close to this persona's planned shape; confirmed (after a real, brief cross-session disagreement, resolved by re-reading the source wording directly) that this maps to *this* persona specifically — "scores every spec... before greenlight" is a per-proposed-change gate, not a standing corpus-wide scan, so it does not describe `corpus-stability-agent` below despite an initial instinct otherwise. One real vendor, not a trend — re-read the actual Allstacks description directly once this persona is actually scoped, don't rely on this paraphrase. Full resolution: `08-sectioncontent-architecture-discussion-2026-09-06.md`.
- [ ] Third persona, for demo variety and because real seed data already exists: `corpus-stability-agent` — surfaces the real 42 cross-repo ambiguities as a live finding, not a canned example. Reconnects to ADR-005's long-deferred sanity-report leg. (Not the Allstacks match above — see the note on `impact-analysis-agent`.)
- [ ] Not yet designed: a real, durable persona-file *format* (today's `atomic-prd-agent-persona.md` is real but ad hoc) — worth deciding once 2-3 personas exist and real, common structure is visible, not designed in the abstract first.

## 3. Workflows — hand-seed, don't automate yet

- [ ] Not started. Real, deliberate plan (per prior discussion): 3-5 real workflows, hand-seeded collaboratively (using `walk_cluster` on known anchors, the historic PMO-012-shaped corpus as a *reference*, never a trusted source per its own unverified-content caveat), proving the concept before any self-proposing/self-diagnosing automation. Self-proposing and self-diagnosing are explicitly later-phase, not POC scope.

## 4. Query interface

- [ ] Not decided. Default plan: no custom interface for the POC — demo through whichever MCP-capable client is already in front of you (Claude Code/Desktop). Build something custom only if that's not presentable enough for the actual audience.

## 5. Documentation (distilled narrative, not more internal task-tracking)

Real, distinct task from everything already written this project — the internal docs (ADRs, task lists, findings) are thorough but not demo-shaped. Needs, once Step 6 (item 2 above) actually closes:
- [ ] The real story in narrative form: the problem (retrieval kept missing evidence), what was tried and failed (two hybrid-retrieval experiments, with real numbers), what worked (the reranking test, then the full agent), why this architecture (real Gemini Enterprise findings, real multi-client portability).
- [ ] A simple architecture diagram: P1/P2 (unchanged) → generic MCP tool layer → task-specific personas → real client (Gemini Enterprise / Claude Code / etc.).
- [ ] Real numbers worth keeping ready: the retrieval rank improvements (Q1a #24, the reranking win on Q1b), the 92→42 correction, real embedding/LLM spend totals if asked.
- [ ] **Real, external validation points — added 2026-09-06, important for management specifically, don't drop this.** This project's real architecture choices line up with, and in places exceed, real 2026 industry practice — that's a genuine, citable finding from live research, not self-assessment, and it's exactly the kind of thing that reassures a management audience this isn't a one-off internal invention. Pull directly from `governance/roadmap/market-research/`: (a) the retrieval-architecture pivot matches Claude Code's own documented move away from embeddings-only RAG, and real production telemetry from GitHub Copilot showing iterative multi-call exploration dominates in practice (`02-findings-retrieval-architecture-2026-09-05.md`); (b) the `SectionContent` document-template design (prose/checklist/user-stories) independently converges with real, unrelated 2026 tools — GitHub Spec Kit and ChatPRD (`04-findings-document-types-2026-09-06.md`); (c) this project's fact-level citation discipline is *more rigorous* than what real 2026 tooling/research describes elsewhere for this purpose — a genuine differentiator, not a gap (same doc); (d) the Gemini Enterprise delivery path (custom MCP server support, Claude running natively in its model catalog) is real, current, and dated the same week this was designed (`01-findings-2026-09-05.md`). Frame this in the demo as "independently validated against real 2026 peer practice," not just "we think this is a good design."

## 6. Slides

- [ ] Blocked on item 5's narrative existing first — same content, different packaging. Once the story is distilled, this is a bounded, mechanical step (an Artifact-based deck), not a research task.
- [ ] Carry item 5's external-validation point into the deck explicitly, likely as its own slide — a real "this matches/exceeds 2026 peer practice" slide, named products and sources included, lands very differently for a management audience than folding it as a footnote into the architecture explanation.

## 7a. Open, not designed: QA's role in this pipeline

Flagged 2026-09-06, same treatment as the known Figma/UX-mapping gap (see project memory "Partial pipeline coverage") — a real, tracked open question, not a decision. Where QA fits hasn't been figured out: as a P1 extraction target (real test files/coverage as their own facts), a downstream document consumer (test plans, acceptance criteria), or a real gate between an agent's proposal and a merge — probably the latter two, sitting after P1/P2 rather than inside them, but not decided. Real, concrete motivation, not speculative: `governance/roadmap/market-research/04-findings-document-types-2026-09-06.md` found that real coding-agent benchmarks (SWE-bench) use a repository's own executable test suite as the actual verification mechanism for autonomous changes — meaning QA/testing is plausibly the literal, real gate this pipeline would need if the auto-merge trajectory discussed the same day ever becomes real, not just a team that reviews output afterward. Not designed further here — a placeholder to revisit, not a task.

## 7. Real things not to forget, beyond the original 6

- [ ] **A rehearsed demo script** — decide in advance which real queries get shown and in what order, so this project's own honesty about real negative results (which is a genuine strength) shows up as a deliberate demonstration of rigor, not an unplanned surprise mid-demo.
- [ ] **A fallback plan already covered by item 1's own bench decision** — the local-only path *is* the fallback; nothing further needed unless the demo audience specifically expects cloud/Gemini Enterprise.
- [ ] **Pin a known commit/state before rehearsing** — the facts index and docs are both still actively changing; rehearse against, and demo from, a state you've actually verified, not whatever's live at that moment.
- [ ] **Data sensitivity check** — real business data (addresses, inhabitant types, real repo internals) will appear in any live query output; confirm who's in the room before showing it.
- [ ] **A real, above-board cost line ready if asked** — this project has tracked every dollar of embedding/LLM spend carefully all along; have that story ready rather than reconstructing it live.
