# Prompt 10 — resolve the resolved-graph builder's stale-ID mismatch

Copy everything below the line into a fresh Claude Code session in this repo. Independent of prompt #9 (capability-fan-out design) — different scope entirely, don't conflate them.

**Standing rule: do not run `git add` or `git commit` under any circumstances.** Leave all changes uncommitted and report back — only the user commits, always.

---

Real, quantified issue, found 2026-09-11 while loading Swift's intra-repo edges (`governance/roadmap/graphrag/01-findings-and-open-questions-2026-09-10.md` §10 conclusion 3, second bullet): a real minority of `sourceCallFactId` values inside `resolved-engineering-graph.json` don't match any real `fact_id` in the `facts` table — for `ios-oskey-dev` specifically, **19% of built edges (1,663 of 8,734)** had to be dropped as dangling (`android-intercom-oskey-io` was only 0.03%; the 4 Swift-kit repos were 0%). This was worked around defensively in `build-intra-repo-edges.ts` (filter out any edge whose source isn't a real, live fact_id before insert — correct, safe, but not a fix for the real cause).

**A first hypothesis (a divergent ID-computation code path inside `02-build-module-evidence.ts`) was checked directly and disproven, don't re-investigate it**: `graphPayload.facts` (which becomes `{module}-evidence-graph.json`) is the exact same, already-deduplicated, already-`stableFactId()`-bounded array that also becomes `{module}-facts.json` — there is no separate code path there.

**The real, confirmed cause is staleness, not a code bug**: `output/runs/ios-oskey-dev/20260910_164201-e660bda2/knowledge-pipeline/modules/*/{module}-evidence-graph.json` files carry `generatedAt: 2026-09-11T07:31:53` (regenerated by the earlier `stableFactId()` fix session's own re-run of `02`). But `resolved-engineering-graph.json` (built by `04-build-resolved-graph.ts`, which reads those same evidence-graph files) is still stamped `generatedAt: 2026-09-10T16:42:41` — a full day earlier, never regenerated since. Confirmed further: none of its `sourceCallFactId` values carry the `[sha1:...]` bounded-form marker, and lengths run up to 3,546 characters — consistent with pre-fix, unbounded IDs. **`04` simply hasn't been re-run since `02` picked up the fix.**

## What to actually do

1. **No code trace or code fix needed — this is a re-run + verify task.** Re-run `04-build-resolved-graph.ts` for `ios-oskey-dev` (`REPO_NAME=ios-oskey-dev`, same invocation pattern already used for the 4 Swift-kit repos in `governance/roadmap/graphrag/03-prompt-7-sync-repos-to-parity-findings.md` Task 2) so it regenerates `resolved-engineering-graph.json` from the current, already-fixed evidence-graph files.

2. **Re-run `build-intra-repo-edges.ts` for `ios-oskey-dev`** against the freshly-regenerated graph file, and check the real dangling `source_fact_id` rate — expect it to drop sharply from 19%, but **don't assume it hits exactly 0%**; report the real number and investigate honestly if any real dangling remains (there may be a smaller, genuinely different cause still present).

3. **Check `android-intercom-oskey-io`'s smaller 0.03% dangling rate the same way — don't assume it's the identical cause.** Confirm directly whether its own `resolved-engineering-graph.json` is similarly stale relative to its evidence-graph files, or whether something else (a handful of genuinely unusual facts, a different edge case) explains its much smaller rate. If it's the same staleness pattern, re-run `04` for it too and re-verify; if not, report what you actually find rather than forcing the same explanation.

4. **Verify for real:**
   - Real before/after dangling counts and rates for both repos.
   - Confirm no regression: the edges that were never dangling still insert correctly, same real counts as before for those.
   - Cross-repo isolation still holds (re-check a couple of the untouched repos' counts, matching the standing verification pattern used throughout this whole batch).

5. **Leave all changes uncommitted.** Report your real numbers plainly, per the standing rule above.

If re-running `04` doesn't actually resolve the mismatch, or something else real turns up, say so directly rather than forcing this explanation to fit.
