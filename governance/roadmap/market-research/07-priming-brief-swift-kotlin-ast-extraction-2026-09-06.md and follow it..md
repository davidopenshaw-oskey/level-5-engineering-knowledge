# Priming Brief — Best-in-Class Tooling and Practice for Swift/Kotlin AST Fact Extraction (P1 Next Steps)

**Purpose of this file:** hand this to a fresh Claude session as its first message (or tell it to read this file first). This project's P1 (deterministic AST evidence extraction) currently covers TypeScript/Angular repos only, using `ts-morph` for real, compiler-grade type resolution — not text/regex matching. The next real repos on the roadmap are Swift (iOS) and Kotlin (Android), and no extraction has started against them yet. This is a research/report task — do not modify any file in this repository other than writing your own findings report (location at the bottom).

## Context — read first, in this order

1. `governance/roadmap/swift-kotlin-preparation/00-lessons-from-typescript-angular-extraction.md` — a real, already-written anticipation document generalizing 9 concrete classes of problem found during the real TypeScript/Angular P1 work (dual UI paradigms, enum/closed-set extraction, recursive nested-type resolution, cross-repo type redeclaration, AST-walker coverage gaps, actor-scoped UI narrowing, camelCase tokenization) into Swift/Kotlin-specific hypotheses. **It states its own epistemic status clearly: these are anticipations from general language/tooling knowledge, not confirmed findings against OSkey's actual mobile repos** — no Swift or Kotlin repo exists in `config/repos.json` yet. This research should try to independently corroborate, extend, or falsify these hypotheses using real, current, external sources — not just take the doc's word for it.
2. `governance/adrs/adr-000.md` and `adr-002.md` — P1's actual extraction philosophy: deterministic AST evidence (not LLM interpretation), fact-based and unmodified (e.g. fully-qualified type strings preserved exactly), with all formatting/presentation deferred to later synthesis layers. Any tool recommended here needs to fit that philosophy, not just "parse the language."
3. `governance/roadmap/market-research/02-findings-retrieval-architecture-2026-09-05.md` — briefly, just the camelCase/compound-identifier tokenization finding referenced in `00-...md` item 8, so this round doesn't re-research something already found.

## What to actually research

**Search live, on the open web, for real, verifiable information — do not answer from training knowledge alone.** Verify anything you report with a real source, and cite it. If something isn't found, or found only via an unconfirmed secondary source, say so directly rather than padding with speculation — this project's own standing discipline (see how `06-findings-product-team-artifacts-2026-09-06.md` flagged an unconfirmed secondary statistic separately from its confirmed primary source).

**A real, deliberate instruction, stated up front because it cuts against how the earlier market-research briefs in this folder were framed: do not filter or weight by recency.** Those earlier briefs (`03-`/`05-...md`) were about fast-moving product features and market trends, where "real 2026" mattered. AST/compiler tooling is a different category entirely — closer to compiler infrastructure than to a product feature — and this project's own precedent already proves the point: `ts-morph` was chosen for TypeScript because of real, demonstrated type-resolution capability, not because it was new. A tool that is 10 or 15 years old and still the actively-maintained, trusted standard is not a weaker answer than something released this year — it is very plausibly the *better* one; this category of tool tends to mature with real-world edge cases fixed over years, not go stale. **The one thing that genuinely does need a real, current check is maintenance status, not age**: whether a candidate is still actively maintained, hasn't been quietly abandoned or superseded, and is still what real, current projects actually reach for — verify that directly (recent commits/releases, current real-world adoption, what its own maintainers or the wider ecosystem point to now) rather than assuming past reputation still holds. Report the single most capable, most battle-tested real option for each angle below, regardless of its age — and separately confirm it's still alive and adopted today, not resting on an old reputation.

1. **The most capable Swift AST/type-resolution tooling available, regardless of its age.** `ts-morph` was chosen for TypeScript specifically because it gives real compiler-grade type resolution, not just syntax parsing — confirmed necessary after bare field-name matching found 25 real false-positive collisions in this project's own work. What is the real, comparably capable, best-in-class option for Swift today? Investigate **SwiftSyntax** (Apple's own AST library) directly — does it alone provide type resolution, or does genuine type-checking require pairing it with **SourceKit / SourceKit-LSP**? Are there real, named projects (not just Apple's own internal tooling) using this combination for large-scale, repo-wide fact/graph extraction, and is it still what real projects reach for today regardless of how long it's existed? Name them, with sources.
2. **The most capable Kotlin AST/type-resolution tooling available, regardless of its age.** Investigate the **Kotlin Analysis API** (built on the K2 compiler frontend) directly — how mature and battle-tested is it for this kind of use (real type resolution across a whole codebase) today, and is it genuinely distinct in capability from lint-focused tools (detekt, ktlint) the way `00-...md` claims? Are there real, named projects using it for extraction rather than linting, and is it still actively maintained and the real current standard, whatever its original release date was?
3. **Dual UI paradigm coexistence, checked against real data, not assumption.** `00-...md` hypothesizes that SwiftUI/UIKit (iOS) and Jetpack Compose/XML-layouts (Android) commonly coexist within one real production app, and that an extractor built for only one paradigm would silently miss the other — directly analogous to the real, measured Angular gap (92 of 160 real `formControlName` usages, 57%, used the missed syntactic form). Is there real, current (2026) survey data, migration-tracking research, or tooling documentation confirming how common this coexistence actually is in real production apps at this point in each platform's migration curve? This directly affects how urgent this specific risk actually is for this project's own extraction design, versus a real but low-prevalence edge case.
4. **Cross-repo/cross-platform model-mirror divergence, checked against real practice.** This project found a real, measured 92→42 cross-repo symbol ambiguity problem between Firebase and Angular (mostly safe mirrors, some genuine collisions). `00-...md` predicts the same pattern recurs between backend and Swift/Kotlin, and speculates a *three-way* version (iOS and Android independently redeclaring the same concept). Is there real, current (2026) tooling or documented practice for detecting/reconciling API-schema-to-mobile-model drift across languages (e.g., contract-testing tools, schema-generation-from-a-single-source approaches like OpenAPI/Protobuf codegen, or drift-detection linters)? If widely-adopted tooling already solves this at the schema-generation stage (i.e., mobile models are codegen'd from one source of truth rather than hand-mirrored), that would be a materially different situation than the hand-mirrored TypeScript/Angular case — check for this directly rather than assuming hand-mirroring is still the 2026 norm.
5. **AST-walker coverage verification, as a named practice.** `00-...md`'s most-repeated technique is a direct grep/regex cross-check of extracted-construct counts against real source, to catch silent AST-walker blind spots (the real BoundText/interpolation gap, the real 92/160 gap). Is this a named, documented practice anywhere in the Swift/Kotlin tooling ecosystem specifically (a coverage-checking tool, a known linter category, a documented technique), or is this project's own home-grown discipline likely still ahead of common practice here too?
6. **Local persistence layers as a third model-mirror.** `00-...md` flags CoreData/SwiftData (iOS) and Room (Android) as a real unknown — a possible *third* kind of local backend-data mirror beyond the network-model layer, with no TypeScript/Angular analog. Is there real, current guidance or tooling on extracting facts from these specifically (e.g., `@Entity`-annotated Room classes, SwiftData `@Model` types), and do they carry the same divergence/staleness risks as the network-model mirrors?
7. **Real integration shape into this pipeline's existing Node/TypeScript orchestration — a research question, not a filter on which tool counts as best-in-class.** This project's whole P1 pipeline (`sync-facts.ts` and siblings) runs in Node/TypeScript, and `ts-morph` was a natural in-process fit only because it's itself a TypeScript library — that convenience doesn't exist for Swift or Kotlin regardless of which tool wins angles 1-2, since SwiftSyntax and the Kotlin Analysis API are native-toolchain (Swift/JVM) libraries with no realistic in-process Node binding. **Do not let this narrow which tool gets named as best in angles 1-2** — name the genuinely most capable option regardless of language runtime, the same instruction as above. Separately, research whether real, existing polyglot extraction pipelines document a working pattern for bridging this gap (most likely: a small Swift/Kotlin subprocess, invoked from Node, emitting structured JSON facts that the existing pipeline consumes the same way it consumes `ts-morph`'s output today) — real prior art for this exact shape, if it exists, is directly useful; if it doesn't, say so plainly rather than assuming one obvious bridging pattern is the only option.

## What a genuinely useful report looks like

For each of the seven angles: name real tools/projects/practices found, with sources, and be explicit about whether it **corroborates, extends, or falsifies** the corresponding hypothesis in `00-...md` — this round exists specifically to convert that document's honest anticipations into either confirmed findings or genuine open questions, not to re-derive it from scratch. Where a hypothesis is corroborated, say what's now more confident than before; where extended, say what's new; where falsified or unconfirmed, say that plainly (e.g., if hand-mirroring turns out to be far less common in 2026 mobile practice than the TypeScript/Angular precedent suggests, that's a real, useful, reportable finding — not a disappointing negative).

**Restate clearly in the report's own framing:** no Swift or Kotlin extraction work is being built yet — this research exists to inform what P1's *next* real design decisions should be once that work actually starts, the same way `02-findings-retrieval-architecture-2026-09-05.md` informed a real architecture decision that hadn't been built yet either.

**Save your findings to `governance/roadmap/market-research/08-findings-swift-kotlin-ast-extraction-<date>.md`** (create the file; don't overwrite this brief).
