# Priming Brief — Does Figma (and Other Design Platforms) Already Expose the Data This Project Needs, Even If Nobody's Built the Bridge Yet?

**Purpose of this file:** hand this to a fresh Claude session as its first message (or tell it to read this file first). The previous research round (`10-findings-sourcegraph-codebase-analysis-2026-09-06.md`, angle 2) asked "does any real code-intelligence company ingest design-tool/UX data" and found an honest, confirmed negative — nobody, even at Sourcegraph's scale, appears to. **This round asks a different, more specific question**: is that because the underlying data genuinely doesn't exist in a usable form on the design-tool side, or because the data exists (via Figma's API, Dev Mode, or its own MCP server) and simply nobody has built the specific bridge this project needs yet? If it's the latter, this project isn't behind an unsolved industry problem — it's ahead of an unbuilt one, and the answer might already be sitting in Figma's own real, current API surface. This is a research/report task — do not modify any file in this repository other than writing your own findings report (location at the bottom).

## Context — read first, in this order

1. `governance/roadmap/facts-serving-strategy/15-workflow-clustering-and-angular-ux-facts.md`, **Part A2 specifically** — the exact, real, still-open gap: a form control's real displayed option/label text (e.g., a dropdown's actual selectable choices as shown to a user) is not fully recoverable from Angular AST extraction alone. This is the precise, concrete thing to check Figma's data against — not a vague "UX data" question.
2. `governance/roadmap/market-research/10-findings-sourcegraph-codebase-analysis-2026-09-06.md`, angle 2 — the prior honest negative this round is directly following up on, from the opposite direction.
3. Project memory context (summarized here since it isn't a file): this project already has a real, planned, in-progress initiative to bring Figma/UX-mapping input into its P1/P2 fact pipeline, expected to start becoming available for most repos around 2026-09-19 — not yet built, and the exact mechanism (Figma's MCP server vs. another method) is explicitly not yet decided. This research should inform that real, near-term decision, not just satisfy curiosity.

## What to actually research

**Search live, on the open web, for real, current, verifiable information — do not answer from training knowledge alone.** Verify anything you report with a real source, and cite it. If something isn't found, or found only via an unconfirmed secondary source, say so directly rather than padding with speculation.

1. **Figma's real REST API — what does it actually expose about a component's real variant/option data?** Investigate Figma's official REST API documentation directly: for a component with variants (e.g., a dropdown, a radio group, a set of button states), what real, structured data is returned about each variant's real name/property values? Is there a real, direct path from "a Figma component named `Dropdown/OwnerType`" to "the real list of option labels a user would actually see" — or does the API only expose visual/geometric data (positions, styles, colors) with option labels only present as unstructured text-layer content requiring separate inference?
2. **Figma's Dev Mode and Figma's own MCP Server — real, current capability, checked directly.** Figma shipped an official Dev Mode MCP Server (real, dated — find the actual announcement and current docs). What does it actually expose to an MCP client: real component structure, real variant properties, real text-layer content, code-connect mappings? Is it positioned by Figma itself as a source of ground-truth UI content (option text, labels), or purely as a design-to-code handoff tool (spacing, styling, asset export) that happens not to carry the specific business-meaningful data this project needs? Check Figma's own docs and any real, current third-party writeups of using it.
3. **Figma Variables / Design Tokens — real business data or purely visual styling?** Figma's Variables feature can hold non-visual values. Does it, in real current practice, ever carry genuine business/domain data (e.g., a real enumerated list of user-facing option values that maps to a backend enum) or is it used in practice almost exclusively for design-system styling values (color, spacing, typography)? This is a direct test of whether Figma's data model structurally *can* hold what this project needs, versus whether it only presents that data as unstructured visual text with no separate structured backing.
4. **Direct test against this project's own concrete gap.** Take the real, specific case already named in `15-...md` Part A2 (an inhabitant-type / owner-tenant-resident-style dropdown, or an equivalent real closed-option UI control) and check, as concretely as real, current documentation/examples allow: if a designer builds this exact kind of control in Figma using real, current best practice (variants, or a variables-driven component), is the real list of option labels then extractable via the API/MCP server in a structured way — or does it still require an LLM/vision step to read text off a rendered frame? Report the honest answer, including "the documentation doesn't go into enough detail to tell, real testing against a live Figma file would be needed" if that's genuinely the case.
5. **Beyond Figma — is Figma alone sufficient to cover, or do other platforms matter here?** Briefly check whether Figma is genuinely close to universal for this kind of work in 2026 (real, current market-share/usage data if findable) or whether other tools (Sketch, Adobe XD, Framer, Zeplin) still hold enough real share that a Figma-only mapping would leave a real, non-trivial gap for this project's actual repos. Keep this angle brief relative to 1-4 — it's a scoping check, not the main question.
6. **Direct retest of the "ahead of the curve" hypothesis.** Having now checked what Figma's data actually contains (angles 1-4), search specifically for any real, named project, plugin, or published practice that already builds a bridge from Figma's structured component/variant data into a code-facts or requirements corpus — the exact bridge this project is planning. If real data availability is confirmed but no bridge is found, report that combination explicitly and plainly: the data exists, unconsumed — a genuinely different, more actionable finding than `10-...md`'s "nobody solves the underlying problem at all."

## What a genuinely useful report looks like

Answer directly, as the report's own clear headline finding: **does Figma's real, current data model structurally contain what this project needs (real, structured option/label text tied to a real UI component), or does it only ever present that as unstructured visual content requiring inference?** Every other angle should build toward answering that one question concretely, with real sources, not general Figma-feature-tour content. If the honest answer is genuinely mixed or uncertain from documentation alone, say so, and be explicit about what a real, cheap follow-up check (e.g., pulling one real Figma file's API response for a known component) would need to look at to resolve it — this project's own standing discipline is a cheap, real, bounded test before scaling up, and that applies here too, not just to code.

**Save your findings to `governance/roadmap/market-research/12-findings-figma-design-data-availability-<date>.md`** (create the file; don't overwrite this brief).
