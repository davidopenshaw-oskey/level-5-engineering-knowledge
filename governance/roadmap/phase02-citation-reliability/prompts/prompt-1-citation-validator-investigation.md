# Prompt 1 — Investigate and decide: how to fix phase-02's own citation-abbreviation reliability problem

**MOOT, superseded 2026-09-18, never run.** Phase-02 is being decommissioned entirely (real DeepWiki architecture research showed dev-facing wiki-style docs need the agentic capability-fanout pattern, not phase-02's older batch synthesis) — see `governance/adrs/adr-010.md` §7 and `[[project_phase02_reports_preserved]]` memory for the full correction. This prompt was drafted but never executed; kept here as a real record of the reasoning at the time, not deleted, per this project's "mark superseded, don't delete" convention. Do not run this prompt.

**Standing rule: never run `git add`/`git commit`, under any circumstance. Writing to files is fine; committing is not your call — only the user commits.**

## Mode: investigation and decision. Not a build. This was explicitly deferred as "a separate, later, real investigation+decision, its own prompt" (`governance/roadmap/graphrag/13-prompt-14-factid-surrogate-key-build-plan-2026-09-17.md`, "Flagged for your own call" item 1) — that recommendation is why this prompt exists, don't skip straight to writing a fix.

## Context — read first

1. `governance/adrs/adr-010.md` — the real, shipped fix for the *equivalent* problem in the mcp-server/PRD-generation flow: a long, natural-key `fact_id` (sometimes multi-line) was unreliable for a model to retype verbatim across a conversation, so it was replaced with a short, opaque, fixed-format `fact_ref` (40-char hex hash) as the actual identity/join key, while the readable `fact_id` text stayed as a display-only field.
2. `governance/roadmap/graphrag/12-prompt-13-factid-surrogate-key-code-inventory-findings-2026-09-17.md` §5 — the real, already-confirmed finding about `citation-validator.ts` (`pipeline/{angular-app-oskey-io,firebase-oskey-dev,node-iot-api-oskey-io}/phase-02-inter-module-synthesis/_shared/citation-validator.ts`): it has a real, deliberate, already-working abbreviation-tolerant fuzzy-match fallback, with its own comment citing an empirically confirmed 2026-08-02 finding that this phase-02 flow's own LLM inconsistently abbreviates long `fact_id` citations (sometimes dropping the file segment, sometimes the trailing value, not at fixed positions). Read `citation-validator.ts` itself in full, not just this summary.
3. **`governance/roadmap/firebase-oskey-dev/02-structural-narrative-synthesis-tiers.md` — the actual primary source, read this in full.** This is where `citation-validator.ts` was originally built (2026-08-02) and where the original bug and fix are documented in real detail: the validator initially found zero citations because this flow cites raw pipe-delimited `fact_id`s, not the `` `file.ts` (lines N) `` style it was first built against; once fact-ID parsing was added, the LLM turned out to abbreviate inconsistently (dropping different segments at different points), fixed with the position-independent token-scanning fallback that's still in the code today. This doc has the real reasoning the later inventory summary (`12-...md`) only referenced secondhand — don't treat that summary as a substitute for reading the original.
4. **Do a real, repo-wide sweep for every other doc touching this**, before concluding your own reading list is complete: `grep -rl "citation-validator" governance/ --include="*.md"` and `grep -rl "2026-08-02" governance/ --include="*.md"` both return a real cluster of related docs under `governance/roadmap/firebase-oskey-dev/` (e.g. `00-capability-based-module-synthesis.md`, `01-cross-module-dependency-graph.md`, `03-token-economics-remediation-plan.md`, `09-fact-table-redundancy-reduction.md`, `10-module-level-production-cutover-plan.md`, `p2-restructure-strawman-critique-target.md`, `tasks.md`) and `governance/roadmap/node-iot-api-oskey-io/01-phase2-contract-design.md` — none of these have been read as part of this investigation yet. Read whichever are actually relevant to the citation mechanism and its real economics/reliability history (the "Economics reassessment" section in item 3's doc, for instance, is directly relevant — real LLM cost data for this exact flow) before writing your recommendation.
5. **Real precedent from the newer `mcp-server/` flow, same problem class, different code path — checked directly, no literal reference exists between them, so treat these as independent confirming evidence, not the same bug reappearing**: this exact failure mode (an LLM abbreviating/mangling a long citation identifier) has now been found three separate times in three separate flows: `citation-validator.ts`'s original 2026-08-02 finding (item 3 above); `mcp-server/agent-poc/validators.ts`'s whitespace-collapse bug (`governance/roadmap/graphrag/09-prompt-12-checkfabrication-whitespace-fix-2026-09-17.md`); and the case-2a content-truncation/transcription-drift bug that directly motivated all of ADR-010 (`governance/roadmap/graphrag/10-case-2a-fanout-content-truncation-finding-2026-09-17.md`, `11-fact-id-newline-reliability-finding-2026-09-17.md`). Read these two fan-out docs — they're real, independent confirmation that this problem class recurs whenever a long identifier has to survive being retyped by a model, which is directly relevant to whether the same root fix (a short, stable reference) belongs here too.
6. `[[project_phase02_reports_preserved]]` memory (or ask the user to relay it if you can't access memory directly) — this phase-02 layer is being deliberately kept for real future HITL "engineering report" value, not dead code. This investigation is what unblocks reviving it for the 3 repos that already have it (Angular/Firebase/Node-IoT).

## The real question this prompt needs to answer

`citation-validator.ts`'s fuzzy-match fallback is a mitigation, not a root fix — it makes the *symptom* (abbreviated citations) tolerable, but doesn't address *why* the model abbreviates in the first place. ADR-010 fixed the equivalent root cause elsewhere by shrinking the identifier itself. **Does the same root fix apply here, or does this flow's real shape make a different fix more appropriate?**

Concretely, investigate and answer:

1. **What does phase-02's actual citation-producing LLM call look like?** Find the real prompt/generation code for whichever phase-02 script(s) produce citations that `citation-validator.ts` later checks (this was never located as part of ADR-010's own investigation — you're the first real look at it). Is the model being asked to retype a long `fact_id` verbatim, the same failure shape ADR-010 fixed elsewhere, or something structurally different (e.g. citing by a different mechanism already)?
2. **Would giving this flow a short reference (the same `fact_ref` ADR-010 already computes and stores) actually fix it**, or does this flow have a real constraint that makes that harder (e.g., does its output format need the citation to be human-readable in a way `mcp-server`'s doesn't)?
3. **What would the real fix cost** — is this a small, contained change (swap what the model is asked to cite, same shape as ADR-010's mcp-server-side fix) or does it touch more of phase-02's own architecture than that?
4. **Real, honest alternative**: if extending the `fact_ref` approach here doesn't fit cleanly, is there a better-targeted fix for phase-02's specific shape — say so directly rather than forcing ADR-010's pattern where it doesn't belong.

## What NOT to do

Don't touch `mcp-server/` or anything ADR-010 already shipped — this is scoped entirely to the phase-02 per-repo layer (`pipeline/{angular-app-oskey-io,firebase-oskey-dev,node-iot-api-oskey-io}/phase-02-inter-module-synthesis/`). Don't write the actual fix in this prompt — investigate, then produce a real, concrete recommendation (and a build plan if you're confident in it), the same two-step discipline ADR-010 itself went through (decide, then a separate build plan, then execution).

## Output

Write real findings to `governance/roadmap/phase02-citation-reliability/01-citation-validator-investigation-<date>.md` — what phase-02's real citation mechanism looks like, whether the `fact_ref`-style fix applies, and a concrete recommendation. If you get far enough to be confident, include a real build plan (mirroring `graphrag/13-...md`'s shape) as a further section — but the investigation and the decision come first, don't skip to code.

Verify claims against the real source and (where relevant) live Postgres before writing them down, same discipline this whole thread has used throughout.
